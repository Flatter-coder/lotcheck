# Dependencies / build output (never committed — see CLAUDE.md)
node_modules/
dist/

# Local tooling
.claude/worktrees/

# Scraper dry-run output
scripts/out/

# OS / editor cruft
.DS_Store
*.log
.gstack/

# scripts/render-report-pdf.mjs writes the rewritten buildReportPdf here so a
# bare `pdf-lib` specifier resolves against this repo's node_modules.
.pdfrender-*/
