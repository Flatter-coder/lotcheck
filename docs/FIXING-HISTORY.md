# Fixing history

A running log of what broke, why, and the guard that now stops the **class** of
it recurring. Newest first.

The point of this file is not the list. It is the **class** column: nearly every
entry below is one of four recurring shapes, and naming the shape is what stops
the next instance.

| shape | what it looks like |
|---|---|
| **Green signal, no check** | a step reports success without verifying it did the thing |
| **Absence read as knowledge** | "we didn't look" rendered as "there is none" |
| **One-surface fix** | a shared bug fixed in one consumer, left in the others |
| **Optional step, fatal failure** | something non-essential takes down the whole request |
| **A count read as a classification** | a surface feature counted, and the tally answered a question it cannot answer |

---

## 2026-09-03

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `PENDING` | **/verify rendered BLANK for every Alberta report issued before 2026-09-03.** The "Insurance before you sign" detail block was gated on the province alone while the line it reads is null below canonical v9, so `fcl.meta` threw; VerifyPage sits outside `ReportBoundary`, so the throw reached the React root. The page whose only job is to prove the paper and the page agree showed nothing. | **Half of a two-step** — the patch that added the version gate used a regex that matched the row and not the block, and the substitution was never verified to have taken. A silent no-op edit. | Both halves of the gate on the block as well as the row, for both AIRB cards, and `check:parity` now pins `financeCoverageApplies(o)&&Number(o.v)>=N&&<div` as a named surface for each — an ungated detail block fails the build instead of a buyer's page. |
| `PENDING2` | **"Your premium after this purchase" shipped** (change of vehicle, the Good Driver cap, the two-million-dollar liability limit). Before merge the review removed an unattributed generalisation — "a cap applies to the increases the AIRB approves, not to what one person pays" — that the report contradicts twice ("moving to a new insurer means drivers are no longer protected by the 7.5% cap"); restored "PPV" to the cap's scope; stopped the card asserting the reader holds a policy; and restored "in 2023" to a truncated quotation. | **Green signal, no check** (two figures were pinned only against a literal typed in our own repo) | `check:citations` gained its own window for this card and now pins 47.1% and the 0.0% order against the dated capture; the capture records that the full 36-page document was searched for a 2026 cap figure and none exists. Canonical v10 marks reports issued with the line. |
| `efa32a7` | **The report said nothing about the one thing a buyer arranges AFTER signing.** A lender requires collision and comprehensive; the AIRB reports Alberta's Take All Comers rule (Insurance Act s. 555) obliges insurers to write only the MANDATORY coverages, and that from early 2024 insurers denied the optional ones to drivers with an at-fault claim in six years or a serious conviction in four. The contract is signed at the dealership; the insurance comes after. Nobody at the desk mentions it. | **Built, not wired** (the regulator publishes it; the report never carried it) | `financeCoverageLine` (report-lines.js) states it in five clauses, every one attributed to the AIRB and quoted in a DATED CAPTURE (`docs/airb-2026-capture.md`, read 2026-09-03); `check:citations` fails the build if the card prints a statute section, regulation, bulletin or page number the capture does not carry, in either direction — the first version of that test compared the citation string with itself. Both halves always ship together (2024 restriction AND the October 2025 correction). It never says a buyer may be unable to insure: the AIRB's own consequence is shopping, and a test pins that. Alberta only, on all five surfaces, with the gate pinned by `check:parity` on the emailed surfaces too. Canonical v9 marks reports issued with the line so /verify cannot add a section the buyer's own PDF lacks. The dealer-APR field is deliberately not a trigger — it once stated 25% for a page that disclosed none. |

## 2026-09-02

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `f9ada19` | **The buyer could not see what an older one of the same car asks.** A regret compilation (567k views, 2026-08-23) is full of owners who financed a new vehicle without ever pricing the model year below it; LotCheck held the answer -- the market page's model-year ladder -- and never put it in the report, where it was neither sealed nor dated nor like-for-like. | **Built, not wired** (an instrument that existed on one page and reached no report) | `olderYearsLadder` (market-count.js) states one model year per rung under the comparison card's own rules: same powertrain (page fuel type read too), the 30-day window on the Alberta clock, at least five listings per year, the price-outlier trim inside each year, the scope that states the most years, and a 4-second bound on the read. Every year read is accounted for: it becomes a rung or it is NAMED with how many were read and how many the outlier trim left, so "no single model year had five or more" can never be printed over a year that had six. A kilometre range covers only the listings that show a reading and says how many did. A pool at the RPC size limit states nothing (the cheapest rows come back first, so every middle would print low). `olderYearsLine` (report-lines.js) words it once for the scroll view, sidebar, emailed HTML, PDF, share link and /verify; the tile carries one figure and the lines carry every year. `OLDER_YEARS_HAS_BASIS` (invariants.ts) demotes any rung that cannot say what it is of. Canonical v8 seals the whole ladder. `test:report-lines` 178 cases incl. the /verify round trip; the copy sweep now bans "worth", "forecast" and "depreciat" outright. |
| `2a1506d` | **"USED MARKET VALUE $69,898 — $9,908 above the local middle value" on a 2026 Lexus RX 350 Luxury AWD with 12,270 km, measured against eleven used RXs that included 2024 hybrids at 80,000 km** (LC-0F75-A93, lexusofroyaloak.com, 2026-09-02; Vic: "how did you come out with this price 9k over asking price?", then "not easy to understand"). `computeBand` fell back to all trims below 5 same-trim rows, dropped the mileage window below 5, took ±2 model years and had no powertrain wall — and the sentence stated none of it. Same day, the emailed card 04 ran labels into values ("Price vs MSRPPRICE UNVERIFIED"): the ten-point rows were `display:flex`, which Gmail and Outlook drop. | **Green signal, no check** (a "comparable" set nobody had made comparable, printed as the market) · **One-surface fix** (the powertrain rule reached MSRP in 4af51be and never the value band; the flex fix reached the deck cards and never the ten-point rows) | `likeForLikePool` (market-count.js) is the one place a comparison set is chosen: same powertrain (the page's own fuel type read too), the count line's 30-day window on the Alberta clock, the subject's model year then one either side, a mileage window for a used subject (no odometer = no comparison, never a 0 km car), same trim → trim family → all trims, floor 5, and every basis figure — dealers, read dates, model years — from the rows behind the sentence. `marketCompareLine` (report-lines.js) words three plain lines (this vehicle / similar listings / difference) under a green, amber or red light on every surface, refuses an unverified or finance-contingent price the way the count line does, and prints a pre-v7 seal as the middle alone, never named from the subject's vehicle string. `MARKET_VALUE_HAS_BASIS` (invariants.ts) demotes a middle that cannot say what it is of. `test:report-lines` (139 cases) round-trips the canonical in the /verify call shape; `check:parity` pins the /verify `fcx` pass; `check:no-emoji` scans the email function; canonical v7. Deploy note: a report sealed under v6 and emailed after this deploy fails verification (422) until it ages out — fail-closed, by design. |
| `7c34bd1` | **The SM360 feed's payment frequency was never read; every feed-sourced financing object was stamped `paymentFrequency: "monthly"`.** A live VW feed (tazaparkvw.com, 2026-09-02) carries `paymentOptions.finance.paymentFrequency: 52` (weekly) with a $196.48 payment over 84 months and a $75,093 total obligation; the math check multiplied 84 x $196 as if monthly, got a ratio of ~4.5 against the total, and the report printed **"Numbers don't add up"** plus a leverage bump against the dealer -- a false flag on a number the dealer published correctly. | Green signal, no check | `captureSm360Financing` maps the feed's per-year frequency (52/26/12) and leaves it null when unmapped, and `computeFinancingCheck` declines to run without a known frequency. Same commit: the two new count/default lines (`market-count.js`, `page-default.js`, `report-lines.js`) ship with `test:report-lines` (95 cases on real captured pages), two invariants that demote an unbacked count or a model-sourced default to *unchecked*, parity anchors for every surface, and `check:cache-ver` coverage of the four new modules. |
| `1dc6ef9` | **A Cloudflare-walled EDealer page (lexusofroyaloak.com, 2026 Lexus RX 350) died with "we couldn't read that page" while its static HTML held the whole vehicle.** `api_usage_log` breadcrumb: Nimble returned a 74-char shell, the direct fetch from the edge runtime was blocked, and the one path that beats a wall — Scrapfly's real browser on residential IPs — was cut off by OUR AbortSignal twice. The 70s pre-warm ran attempt 1 as viewport screenshot + auto_scroll + 8s wait, burned the whole budget, so the HTML-only retry never ran; the rescue rendered fresh and burned 70s more. ~150s on two renders inside a 140s request, over a page whose JSON-LD carried price $69,898, VIN, condition and odometer. Scrapfly kept rendering — and billing — after we hung up, because no server-side `timeout` was sent. | **Optional step, fatal failure** (the screenshot is optional; it consumed the budget that the HTML needed) + **Absence read as knowledge** (an HTML-first attempt would have accepted a bot-wall interstitial as "the page" without a guard) | `_shared/scrapfly.ts`: the ladder is inverted — HTML-only first on a bounded 25s slice, viewport shot as the retry, never fullpage. `isWalledShell()` refuses a challenge page the way Nimble refuses "content too short". Scrapfly's own `timeout` rides with every request, just inside ours. `scrapfly-render.test.ts` 22/22, including the Lexus case (a wall on the HTML attempt falls through to the screenshot); mutation-tested — disabling the guard turns that case red. Checked and ruled out: the Tuesday 08:41Z `published-msrp` job renders one page at a time, so it held one of five Scrapfly slots, not all of them. |
| `88bf38e` | **Logged retroactively (2026-09-02).** The 2026-07-20 fix that only ever lived as a code comment: centaursubaru.ca (also EDealer) timed out on Nimble's vx8 path three times (~a minute of nothing) while the same URL succeeded immediately on vx6. Not the same class as `1dc6ef9` — that site was reachable; this one is walled — but it is the first EDealer read failure and the reason `grep lexus` finds nothing when the next one hits. | **Optional step, fatal failure** (a slow extractor path ahead of a working one) | vx6 is tried first in the Nimble ladder. No test pins it. |
| `febc588` | **Light mode had no page ground.** The Quote Check root set `background: C.paper` and then `backgroundColor: undefined` for light; React applies the shorthand, then removes the `background-color` longhand for the undefined key, clearing the colour the shorthand had just set. It only looked right while tall idle content covered the dark `<body>`; the short error view exposed it — dark page, cream nav, dark-ink title on a near-black ground (Vic's screenshot after the Lexus read failure). | **Green signal, no check** (the light palette "worked" in every screenshot that happened to be tall) | `backgroundColor` is set in both themes. Verified in the pane: light + error state paints `#FBF5EC` with a readable title; dark unchanged. |
| `4af51be` | **A gasoline 2026 Lexus RX 350 was shown the RX Hybrid / Plug-in Hybrid trim ladder as "the factory range the quote should be read against"** (lexusofroyaloak.com, report LC-0F75-A93, 2026-09-02; page: Fuel Type Gasoline, 2.4L 4Cyl), with "asking sits above 2 of 10" computed across the wrong car's prices. The exact-trim pick correctly refused (MSRP "—"); the ladder leaked around it. Three holes, one class: (1) DATA — the catalog held ten hybrid/PHEV RX rows and NO gas RX, because Lexus's TCI feed lists the RX 350 packages under series "RX" tagged "Hybrid Available", `inferFuel()` flattens that to Hybrid, and the refresh guard then correctly refuses a whole gas line tagged Hybrid (the "RX Hybrid" sibling proves the mis-tag) — so nothing is written, every night. Confirmed in a full dry run: `REFUSED: Lexus\|RX\|2026 — 6 trims ALL tagged Hybrid`. Same shape as the TX. (2) CLIENT — `useTrimRange` failed OPEN: known powertrain + zero matching rows → "show all, labelled". (3) SERVER — `applyVerifiedFuelType` "verified" a car's fuel from the first catalog row whose model ilike-matched, and the page's own `fuelType` declaration in JSON-LD was never read. | **Absence read as knowledge** (no gas rows became "here is the range") + **One-surface fix** (`powertrainCompatible()` guarded the trim pick, never the ladder or the fuel check) | `tci-overrides.mjs`: seven Gas rows for Lexus RX 2026 — the feed's own six ex-freight package prices plus the Premium base the feed refused as internal grade "STD", pinned by arithmetic (each feed row minus Lexus.ca's published package delta lands on 60,885 to the dollar; 64,236.18 − 60,885 = 3,351.18, the identical Alberta fee stack that closes the TX 350). `useTrimRange` fails CLOSED with a `none_for_powertrain` state, and the "sits above N of M" claim is suppressed on any mixed pool. `applyVerifiedFuelType` keeps only `powertrainCompatible()` rows, verifies only if they agree, never overrides a page-declared fuel; `jsonld-vehicle.js` extracts `fuelType` and it rides in both gap-fills. `test:tci-overrides` 18/18 (RX case), `test:jsonld-vehicle` +5 (Gasoline→Gas on the node and inside `vehicleEngine`, Plug-in→PHEV never Hybrid). Verified live after a triggered refresh: 7 gas RX rows in `msrp_catalog`, 10 hybrid/PHEV untouched; edge functions deployed on this SHA; bundle `app-751dba8e.js` carries the fail-closed copy. Known follow-ups: override rows carry `fetched_at` 2026-08-26 (stale stamp on hand-seeded rows); the catalog labels RX 350h and RX 500h as one "RX Hybrid" series. |

## 2026-09-01

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `0a744dd` **✓ FIXED** | **We told a buyer there were no itemised fees on a page that itemises them.** The Acadia report read *"Add-ons & fee audit — NONE LISTED / No dealer extras were itemized."* The page prints Market Price **$51,999**, Doc Fee **+$899**, AMVIC fee **+$10**, Dealer Discount **-$3,009**, Your Price **$49,899** — reconciling to the cent. The **$899 documentation fee is the most negotiable line on the whole worksheet**, is exactly the data [[fee-catalog]] and [[used-market-fee-tracking]] exist to collect, and we handed it back. The leverage score was then computed from "no flagged fees", so the miss propagated into the number the buyer reads. **Cause:** that report came off the JSON-LD fallback, and a fee box is rendered HTML, not schema.org markup — the fallback already reads days-on-lot and price-gating out of the same html and simply never read this | absence read as knowledge — and the report KNEW it was incomplete: its own bottom line said *"built from the page's structured data rather than the full page"*, and it published the gap as a finding anyway | `_shared/fee-ladder.ts`, whose entire design is one property: **a ladder is only claimed when the arithmetic closes.** base + additions — deductions must equal the advertised total to the dollar, so a mislabelled line, a monthly payment or a neighbouring vehicle's price makes the sum miss and NOTHING is published. **That check caught the parser's own bug before a buyer could**: the first draft required 3+ digits in an amount and skipped `+$10`, the AMVIC levy on the very page it was written for — the ladder missed by exactly $10 and correctly published nothing. A regulator's levy is classified `regulatory` and excluded from the audit, because telling a buyer to negotiate a fee the dealer cannot waive wastes the one conversation they get. Verified against three real dealer ladders with three different wordings. Wired on BOTH paths — the fallback, and the main scan as a backfill firing only when the extractor found nothing. `fee-ladder` joins OUTPUT_SHAPING. 23 cases, mutation-checked both ways |
| `0a744dd` (2) **✓ FIXED** | **"We looked and found none" and "we never saw the pricing section" were the same sentence.** Any report built without reading the page's price area printed **NONE LISTED** — a clean bill — rather than admitting it could not look | a gap rendered as a fact | `feesRead` records whether the page was actually read; the point falls to **NOT READ** with copy saying plainly it is a gap in our read and not a clean bill. All three states ship to BOTH surfaces, enforced by `check:parity`. `feesRead` is deliberately NOT added to the signed canonical — the substance it describes is already canonical and signed, and adding a provenance flag would change the canonical shape for every report signed before today |
| `ad477ed` | **A used listing came back with `msrpBasis: "exact"` and the report told the buyer a years-old vehicle was thousands "under MSRP."** A fabricated bargain claim that flatters the dealer at the buyer's expense — the inversion of what the product is for; it reached a customer (Advantage Ford, used GMC Acadia, 08-31). The used-vehicle guard was a copy-pasted expression in five places and MISSING from the branch that runs when the dealer's page states an MSRP. Worse, `enrichAnalysis` settled the basis ~120 lines BEFORE the structured-data gap-fill supplied `vehicleCondition`/`odometerKm` from JSON-LD / Convertus / D2C — the basis was decided before the page's own "used" flag arrived. Every signal that should have caught it was pointed backwards: `CATALOG_MSRP_BASIS_LABELLED` flagged the *correct* used report and stayed silent on the wrong one; `deriveCheckpoints` scored the defective scan green; the only regression test re-implemented the rule locally and stayed green on changed code. | **One-surface fix** (the guard landed on the upload path, never the listing path) + **Green signal, no check** (three inverted signals) | ONE decision point, `_shared/msrp-basis.ts` (`msrpIsPresentTense`, `applyConditionToMsrp`): new and demo are present-tense, certified/used are not, unknown fails CLOSED. Identity facts fill BEFORE enrichment. Invariant and checkpoint condition-aware. `test:msrp-basis` imports the real module; making "used" present-tense turns three gates red. Adversarial review caught my own first version failing open on `Number(null)===0`. |
| `6efb1d9` | **MSRP Alerts was retired, but its database door was never closed — and it turned out it never had been.** All eight `revoke … from public` lines across four alert migrations were no-ops (Supabase grants `anon` explicitly; PUBLIC is the implicit pseudo-role — this repo had already documented that at `20260814_lock_service_role_functions.sql`). Verified live with the anon key that ships in page source: `fn_alert_subscribe` ran its body for an anonymous caller (a world-callable write into a PII table), `fn_admin_alert_folders` was executable with only an in-body admin check between anon and every buyer's raw email, and the table carried anon SELECT/INSERT/UPDATE/DELETE with RLS-and-no-policies as the sole control. | **Green signal, no check** (a revoke that reported success over an open door) | `20260901_retire_msrp_alerts_grants.sql`: revokes by NAME from `pg_proc` (two `fn_alert_subscribe` overloads; REVOKE has no IF EXISTS), a catalog sweep for anything touching the PII table, and a POST-CONDITION assert via `has_function_privilege` that aborts if anything is still reachable. Nothing dropped; re-grants in the header. Probed after apply: 42501 / PGRST202 on every retired object, control table still 200. Separate open item: `public.alerts` is anon-readable and no migration created it. |
| `7d2ff22` | **`test:price-index` was green on a page it no longer described.** It grepped `public/live-price-index.html` as TEXT, and after the board was stripped all 29 assertions kept finding their strings in dead code — including honesty copy ("not a representative sample") that rendered nowhere. The hero also still promised the catalog was "checked against what Alberta dealers advertise" after the read behind that claim was removed. | **Green signal, no check** (a gate certifying copy the reader could not see) | 1,401 → 493 lines of dead painters/CSS removed. `scripts/test-price-index.mjs` splits markup from script so a claim about what the page SHOWS cannot be satisfied by a string in the source, and pins id-existence (every `getElementById` must exist in the markup). Mutation-tested 10/10. |
| `ecc7f85` | **`check:copy` listed a deleted surface and still reported "6 of 6 rules clean."** `SURFACES` named `src/PlanetAlerts.jsx`; after the file was removed the gate printed `not found, skipped` and exited 0 — so any renamed or deleted surface silently dropped out of copy-compliance scope under a green result. Found by the removal's own verification pass, alongside two retired routes soft-404ing to the homepage and JSON-LD still selling "price alerts" at $9.99. | **Green signal, no check** (warn-instead-of-refuse) | An unreadable `SURFACES` entry is a hard failure. Mutation-tested: a bogus surface exits 1, removing it exits 0. Retired routes 308 to `/`; the JSON-LD offer no longer names a feature that does not exist. |

## 2026-08-31

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `f6f356d` **✓ FIXED** | **We told a buyer the days-on-lot was not published, on a page that published it.** The delivered Acadia report read *"Days on lot: Not published — ask the dealer"*. The listing's JSON-LD — the same blob the scan had already parsed for price, VIN and odometer — carried `"purchaseDate":"2026-07-30T11:27:42.000"`, which was **32 days** at scan time. schema.org `purchaseDate` is the date the CURRENT OWNER acquired the item, and on a dealer's own listing that owner is the dealer, so it is the inventory date. **Nothing in the repo read the field.** [[days-on-lot-own-engine]] is our own leverage feature and it was handed back for want of one line | absence read as knowledge — a field we never looked for, reported to the buyer as a fact about the dealer | `fillFromJsonLd` sets `daysOnLot` from it, and ONLY when no platform feed already answered — SM360's `date_on_lot` and Convertus's `date_added` are the dealer's operational record and keep priority. Guarded both ways (a future date and one older than ten years are data errors, not vehicles) and reported from **one full day**, because "0 days" is true, gives a buyer nothing, and is indistinguishable from a mis-stamped date. The claim names its own source. 9 cases in `test:jsonld-vehicle`, mutation-checked: publishing from 0 days turns it red |
| `f6f356d` (2) **✓ FIXED** | **Every identity check compared the page against itself, so a response that was wrong but self-consistent passed all of them.** A CDN serving a stale or mis-keyed page, a silent redirect, a relisted stock number — each returns a body whose JSON-LD, price and odometer agree with each other **about a vehicle the buyer never asked about**. The report reads perfectly and is entirely about the wrong car. That is [[ai-defamation-entity-match-lesson]] exactly: a wrong entity match, stated as fact, relied upon — and no check in the function could see it, because they all interrogate the same response | no single point of failure — every identity signal came from one source, so corrupting that source corrupted the verification with it | Many dealer platforms name the VIN in the path (`/inventory/used-2025-gmc-acadia-1gkennrs2sj181578/`) — **the one identity claim the response cannot forge.** When the URL names a VIN and neither the page text nor its markup ever mentions it, the scan refuses and releases the credit. Deliberately narrow, because a false refusal costs a real buyer a real scan: path only (not the query), rejects I/O/Q, rejects all-digit and repeated-character placeholders, and silent when the URL carries no VIN — including the Advantage Ford link that prompted it. **Written from an UNREPRODUCED report:** two agents saw Fox Dealer hosts serve another dealer's vehicle at the right URL; five of my own fetches across two of those hosts returned the correct VIN every time. It ships because the failure is catastrophic and invisible to everything else, not because the CDN bug is confirmed. 14 cases, mutation-checked. `check:error-codes` caught it shipping without a client branch |
| `ab51ce8` **✓ FIXED** | **The anti-bot render asked Scrapfly to parse dealer HTML as JSON, so the rescue path could never produce a readable page.** `format` means two different things on Scrapfly's two APIs: on `/screenshot` it is the image format (`jpg`/`png`), and on `/scrape` it is how to interpret the PAGE. `scrapflyRenderOnce` sent `format=json` to `/scrape` — so every walled host came back as 473KB of Scrapfly's own JSON envelope with **no `<script>` tags at all**, and every structured reader we own (JSON-LD, `__vdpJSON`, `vmsData`, EDealer `vehicleArray`) reads inside a `<script>`. The render is the fallback that exists precisely for the ~28% of Alberta hosts that wall our datacenter IP, and it was returning something no reader could read, on every one of them. This is what Vic's Advantage Ford link kept failing on, three times. **Proven live against the vendor**, same URL, one parameter apart: `format=json` → `content_type: application/json`, `extractJsonLdVehicle` NULL, exactly matching production's `jsonLdVeh=null convertus=null d2c=null` breadcrumb; `format=raw` → `text/html`, 1,324,471 chars, `extractJsonLdVehicle` → **2025 GMC Acadia AWD Elevation, $49,899, 50,550 km, VIN 1GKENNRS8SJ240715**, and `jsonLdVehicles` counts exactly one vehicle | built-but-unwired — the rescue existed, was called, was billed for, and could not work | `scrapfly-render.test.ts` asserts against the SOURCE that the render requests `format=raw`, mutation-checked (restoring `json` turns it red). The trap is named in a comment at the call site, because the same word meaning two things across two APIs is not something the next reader can infer |
| `b447837` **✓ FIXED** | **53 live Alberta websites were recorded as unreachable because we guessed the wrong URL.** Re-probing the 336 hosts that never answered the province-wide probe: 144 domains do not resolve at all (the business is gone), 55 answered on a patient retry, and **78 failed on TLS — of which 53 answer perfectly well**, 47 over plain `http://` and 6 on the www-flipped name. AMVIC records a website as it was TYPED, so the scheme and the `www` prefix were always our guesses, and each failed guess was written into the catalogue as a verdict about the dealer | absence read as knowledge — our own request recorded as the world's answer, the same shape as *EDealer: 0 across 1,639 Alberta hosts* | `originVariants()` in `_shared/dealer-catalog.ts` beside `toOrigin`, so the probe and the scanner share one definition instead of growing copies that drift. It fires **only on a transport failure** — a host that answered anything, 200 or 404 or even 403, has told us something, and re-asking on another scheme spends requests to hear it twice — and on a hit it carries the origin that WORKED, since recording the dead one sends every future probe back to it. It cannot invent a hostname: an IP or a dotless name gets no www form. 10 cases, 49 total, mutation-checked. Same commit narrows `check:cache-ver` so a `*.test.ts` edit no longer demands a full cache wipe — a gate that cries wolf is a gate people route around |
| `22cd8e4` **✓ FIXED** | **The forensics reader filtered out the very fields that were the answer.** `b2ee4b6` had just added `pageSrc` / `jsonLdVeh` / `convertus` / `d2c` to the 502 breadcrumb so a failed scan would say what it was actually holding. The reader that prints those breadcrumbs matched on the OLD field names and dropped every line carrying the new ones, so the diagnostic built that morning showed nothing | green signal, no check — inverted: a tool that reports *no evidence* when the evidence is sitting in the row it just discarded | the reader always prints the raw line as well as its parse, so a field it does not recognise is visible rather than silently dropped |
| `b2ee4b6` **✓ FIXED** | **A failed scan did not say what it had been holding, so three failures in a row were diagnosed by guessing.** A 502 emitted no record of which source answered or which readers found anything, which is why the `format=json` root cause above survived three separate investigations | absence read as knowledge | the 502 breadcrumb now carries `pageSrc`, `jsonLdVeh`, `convertus`, `d2c` and the host, so the next failure names its own cause. Same change measured all 1,639 hosts rather than reasoning about them |
| `941607c` **✓ FIXED** | **No way to reproduce one listing's read without deploying and asking a buyer to try again.** Every investigation into a broken URL was a guess followed by a deploy | test before release — unverifiable by construction | `scripts/probe-one-url.mjs` fetches one URL with the scan's exact parameters and runs every reader over it. `check:jobs` caught it shipping without a User-Agent, which is the rule that exists because anonymous Overpass requests silently lost 4 of 5 weekly runs |
| `f779529` **✓ FIXED** | **Scan breadcrumbs were written and never read.** `api_usage_log` held the record of what each scan did and nothing could summarise it, so the log existed as a claim rather than a tool | built-but-unwired | `scripts/scan-forensics.mjs` reads them, with a workflow so it runs where the service key lives |

## 2026-08-30

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `b76dc66` **✓ FIXED** | **We knew 1.8% of Alberta and rediscovered the rest on every scan.** `dealer_source` held **30** hosts — the ones a feed probe had confirmed crawlable — against a province of **1,639** dealer websites (AMVIC licensees with a website; 21,866 licensees in total). So every broken listing was its own investigation: the Convertus price miss, the D2C gated price, the SM360 feed, the Cloudflare wall. Every fix was real and not one of them reduced the NEXT one's cost, because a scan started from zero knowledge about the host every single time. Vic, after the third consecutive single-site failure: *"i want catalog of all car websites in alberta the structure what ever we need to stop fixing we should have them all listed on place track them daily and read them on as soones lotcheck user makes request"* | the class was never named because nothing held the class — a per-site fix cannot generalise when there is no per-site memory | `dealer_source` becomes the CATALOGUE: one row per Alberta car website, whether or not we can crawl it, whether or not we can even read it. **Loaded and live: 1,622 hosts** (1,585 from AMVIC + 37 pre-existing), against 30 before. A host we cannot read is a FACT worth recording — leaving it out is exactly how *"EDealer: 0 across 1,639 Alberta hosts"* was believed for a day, when the truth was that 452 of those hosts (28%) had refused our datacenter IP. Two fillers, neither of which touches a dealer's server on our own initiative: AMVIC's roster (which we already hold — the loader does no HTTP at all), and **every live scan**, which writes back whether a plain fetch worked, whether the anti-bot pass was needed, and which platform answered. The crawler is untouched — it selects `active` rows on a crawlable platform and catalogue rows land `unknown`/inactive — so cataloguing a host is not crawling it and the standing-crawl legal question stays exactly where it was |
| `b76dc66` (2) **✓ FIXED** | **I hung new work off the single point of failure the repo had already removed — and Vic's link failed again because of it.** The multi-vehicle classification shipped that morning read the page's own declaration from `directHtml`, the ONE fetch Cloudflare walls. `pageHtml` ("direct fetch first, the render second") has existed since `2026-08-27`, written after a real paid report was hollowed out by exactly this block at lexussouthpointe.com — the buyer was shown *"ASKING PRICE: Not shown"* and *"VIN: NOT ON QUOTE"* while the page carried both. I wrote the new read one line above the fix that exists to prevent it | one-surface fix / [[no-single-point-of-failure]] — reintroduced beside its own remedy | the classification and the JSON-LD fallback guard both read the shared source. `test-page-read` already pinned that "the structured readers hang off pageHtml, not the raw direct read"; the catalogue lookup and the subject classifier are now inside that same discipline |
| `b76dc66` (3) **✓ FIXED** | **A 6-agent adversarial audit, run before merging, found eight defects — four of them mine.** (a) `http_error` covered every non-OK status except 429/503, so a **404 on a sold vehicle was indistinguishable from a Cloudflare 403** — one buyer pasting a delisted listing would have pinned that dealer's whole site to the paid render path for a week, and a sold-vehicle URL is the single most common thing a buyer pastes. The status code was captured all along and dropped before it reached the decision. (b) The wall verdict **re-armed its own expiry**: every walled observation stamped `last_direct_fail_at = now()`, and the 7-day re-test measures age from that column — so the safeguard could only ever fire for a host nobody scanned for seven straight days. (c) My first fix for the latency **made that loop worse**: cutting the direct retries from 3 to 1 trades away the only thing that lifts a wall verdict, and the repo's own measurement says how much — at lexussouthpointe 57 of 67 requests returned a challenge shell, so one attempt clears it 15% of the time against three attempts' 39%. (d) The write-back sat inside `pageHtml`'s `.then` under its blanket `.catch(() => null)`, so a throw in the *optimisation* would have emptied every structured reader in the scan — the exact failure `page-source.js` exists to remove, with a comment above it claiming it could not happen | absence read as knowledge (a bad page read as a walled host) / a safeguard that only protects what nobody touches / optional step, fatal failure | a wall is now a **refusal** to serve us (401/403/407/451), resolved from the status code; a 404 or a 500 says nothing about the host. The clock is stamped when the wall is **first** seen. Every free attempt and its spacing survive — what the catalogue shortens is the per-attempt **timeout** (15s → 6s on a known-walled host), because a host that refuses us refuses us fast, so an attempt still running at 6s is hanging, and three hanging attempts spent 45s of a 140s budget re-learning something recorded. The write-back is wrapped so nothing in it can reach that `.catch`. **The gate encoded one of these rather than catching it** — its "a 403 counts as a wall" case asserted against `http_error`, the same value a 404 produces — and now pins both directions, 39 cases |
| `b76dc66` (4) **✓ FIXED** | **`revoke ... from public` is a no-op, and this repo had already proved it — I wrote it anyway.** `20260814_lock_service_role_functions.sql` exists precisely because that pattern left `fn_capture_quote` — the CREDIT CAPTURE function — reachable with the anon key shipped in the client bundle: PUBLIC is the implicit pseudo-role and Supabase holds separate EXPLICIT grants to `anon` and `authenticated`. Three new SECURITY DEFINER functions went out with the same three lines, one of them a WRITER that would have let anyone pin any Alberta dealer to the paid render path. Separately, the observation would have **routed around the licence gate**: `dealer_source` is the roster the standing crawl would run against, and its AMVIC-licence invariant is part of what the pending legal sign-off would be given for — live traffic filing whatever host a buyer pasted, with province defaulting to `'AB'`, would have broken it | one-surface fix — the same defect the repo had already named, in a file written to prevent it | revoked from `public, anon, authenticated`, and a `do $$` block **fails the migration** if any of the three is still executable by anon (a revoke that silently failed is how this went unnoticed for weeks the first time). Verified in production after apply: all three return `42501 permission denied` to the anon key, as does `dealer_source` itself. A new row is created only for a host matching an **Issued** AMVIC licensee; an unlicensed or out-of-province host teaches us nothing and takes the default ladder. Also: `operation: "render"` had been declared and unused, so the admin panel's per-scan cost was the screenshot alone — a change to how often the render fires would have drifted the panel from the Scrapfly invoice invisibly. It carries a ledger row now |
| `b76dc66` (5) **✓ FIXED** | **`check:migrations` read only the FIRST column of a multi-column `ALTER`.** Postgres takes a comma-separated action list, so `alter table t add column a, add column b` really does add both — the gate registered `a`, then failed a later insert naming `b`, pointing at a fault that was its own | green signal, no check — inverted: a red signal, no fault | the gate reads the whole statement to its terminating semicolon and takes every `ADD COLUMN` inside it. **Mutation-checked**: a bogus column in an insert still turns it red, and the new columns are all tracked. Also killed a duplicate: `toOrigin` existed in the probe and was about to be written again for the scanner, so two copies would have drifted and a host the probe catalogued would be one the scanner could not find. One definition in `_shared/dealer-catalog.ts`, imported by both, verified unchanged against OSM (468 dealers → the same 131 usable hosts the probe has always reported) |

## 2026-08-27

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `f42b49e` **✓ FIXED** | **A real vehicle detail page was refused as an inventory grid for mentioning its neighbours.** Vic pasted a live Advantage Ford listing and got *"Sorry, we can't process a page with multiple vehicles. This looks like a search-results or inventory page"*. It is one vehicle's own page. Measured in-browser before touching anything: 8 VIN-shaped tokens, **six** valid — the 2025 GMC Acadia it is for (`1GKENNRS8SJ240715`) and five Fords and a Mazda in a similar-vehicles rail below it. The rule was "more than one checksum-valid VIN means a grid". Vic's correction of my framing, and the principle the fix implements: *"its multiple pictures of the same vehicle not multiple vehicle's ... you need verification process first establish what's on webpage then create report"* | **a count used as a classification** — how many vehicles a page MENTIONS is not how many it is ABOUT | the page is CLASSIFIED first, from its own machine-readable declaration (schema.org vehicle nodes, or the platform's vehicle-data blob), and only then is a report built and **pinned** to the vehicle that declaration names. One declared = a detail page however many it mentions; several declared and none of them this page = a grid; several found and none declared = a grid with no structured data, refuse. That evidence costs nothing — the direct HTML fetch is already in flight from the top of the scan for three other reasons. **The checksum guard earns its keep in the same measurement**: the two tokens that FAIL are `16V4H13V16H11ZM14` and `16V4H15V16H14ZM17` — SVG `<path d="…">` coordinates with the spaces stripped, so two icons alone could push a genuine single-vehicle page over any count threshold. Many PICTURES of one car, and the same car repeated across cards, are first-class pinned cases in `test:multi-vehicle` (58, offline), not an accident of using a Set. New `_shared/multi-vehicle.ts` owns the counter and the decision, with the test coverage the inline version never had |
| `f42b49e` (2) **✓ FIXED** | **Accepting such a page is only safe pinned, and a 6-agent adversarial audit run BEFORE merging refuted the fix twice and found twelve defects in it — nine of them mine.** The sharpest: **rail cards do not print VINs**, so a read off a neighbour returns NO VIN, which the VIN comparison scored as "no mismatch" — and the gap-fill sixteen lines later then stamped the DECLARED subject's VIN onto it, manufacturing a report that looks internally consistent (right VIN, valid check digit) while its price, trim and odometer belong to another car. Also: the VIN is the field a wrong read is most likely to get RIGHT (stated once at the top), while year and make come off whichever card was read — and those drive the recall lookup and the MSRP denominator; the two new `await`s put a 15s retry ladder in front of the extraction and could have reached the 150s Supabase kill that **strands a credit hold**; bounding them then created a worse bug, where a lost race made a real VDP look undeclared and gave it a 2h/24h lockout — the reported bug made durable; the guard armed off truncated markdown while the declaration came from full HTML, so it silently disarmed on exactly the pages it exists for; and it lived inside a `catch` whose stated policy is never to sink the scan | one-surface fix / [[audit-your-own-fix-same-night]] | the pin is three questions, not one — the VIN must not contradict, the IDENTITY must not contradict (`identityMismatch` on year and make, only where both sides have a value), and something must corroborate, so a read with neither a VIN nor a matching make is unpinned and refused. Both awaits are raced against a bounded settle; the verdict separates "this page is a grid" from "we never got a look at it", and only the first may write a lockout; the guard arms off the union of both page sources; and it now sits OUTSIDE the swallowing catch — safe by structure, not by luck |
| `f42b49e` (3) **✓ FIXED** | **The other half of the audit hunted the OPPOSITE bug — real pages the fix still refused — and found four more, three of them new in my fix.** A page with no schema.org markup takes its declaration from the platform blob, and that object carried **no make and no year**, so the corroboration arm read OUR OWN missing field as "nothing ties this read to the page" and refused a whole platform family after the full spend, every scan, with an escalating lockout each time. Every Convertus VDP ships a hardcoded, checksum-VALID VIN as the **trade-in form's input placeholder**, which counted as a second vehicle and armed the guard on 100% of that platform. The declaration reader answered "which VINs" and never "how many vehicles", so a detail page declaring one vehicle whose VIN string is unusable scored "declares nothing" and was refused. And platforms that mark up their similar-vehicles rail **properly** — a Car node per card — were punished for it | absence read as knowledge, three times over: our own missing field, our own unread fetch, and our own unusable string, each rendered as a fact about the buyer's page | the blob carries year and make (both already in hand a line above) and the corroboration arm fires only when a declared make EXISTS and the read has none; form chrome is stripped before counting; the reader returns the NODE COUNT (a detail page is a detail page whether or not its VIN is readable — the pin simply does not arm), reads `mpn` as a fourth VIN field, and dedups a unit described twice; and the node whose `url`/`@id` IS the scanned page is the subject, the rest are the rail. **And when we simply could not read the page, the refusal said "This looks like a search-results or inventory page"** — an unbacked claim about the buyer's page, the same defect as the rest of this change aimed at them. It now says what is true |
| `f42b49e` (4) **✓ FIXED** | **Three siblings, all pre-existing, all found by the audit.** (a) The refusal could not REACH the buyer: `subject_mismatch` had no client branch, and the 422 handler reads the body once — so an unrecognised code fell through to a second `res.json()`, threw *"body stream already read"*, and turned a message that says "you haven't been charged" into a generic failure. (b) The repeat-attempt throttle was consulted BEFORE the classification, so every URL recorded under the old count rule stayed locked out for 2h/24h under a verdict we no longer reach — **starting with Vic's own link**, already on the counter from the refusal that prompted this. He would have installed the fix and still been unable to scan it. (c) `subject_mismatch` itself had no throttle at all, despite landing after the page fetch, the sealed screenshot and the paid extraction, and being deterministic for a given page — so it repeats rather than self-heals | one-surface fix / green signal, no check | (a) fixed for that code and every future one — the 422 block answers from the body it already has — and the durable fix is not another sweep: new **`check:error-codes`** enumerates every code the two functions can return and fails if any has no branch in the app (8 codes, all rendered). (b) the throttle is consulted only once we still mean to refuse, which is also where it belongs. (c) the expensive refusal records a hit like the cheap one does. Separately, the JSON-LD fallback path now classifies before it charges: `extractJsonLdVehicle` takes the FIRST priced node and stops, so on a grid whose page fetch failed it was building a complete, enriched, sealed and CHARGED report about whichever card was first in the markup |
| `59aa00d` | **"Too large to photograph" was answered by photographing LESS of the page.** `captureListingScreenshot` asks Scrapfly for `capture=fullpage`, gets the whole page, and discarded it whenever its base64 ran past `1_500_000` chars — re-shooting the VIEWPORT instead, so the report shipped a photo of the TOP of the listing. The cap was a guess: `fb67429` justified it only as *"size-capped so a giant full-page render never bloats the response/cache"*, against no downstream limit at all. And it was not a ceiling on giants — at 1920 the failing Mazda VDP is **11.5 MP**, a page JPEG runs ~0.05–0.15 bytes/pixel, so the cap sat in the middle of the ORDINARY tall-dealer-page band. The degrade was not the rare path it was written as. Vic: *"scrapfly only took screnshoot half the page"* | absence read as knowledge — a file we chose not to carry, rendered as a page that could not be photographed | the cap is DERIVED and is the smallest of **four** real limits, tightest first: the email body cap (`MAX_BODY_BYTES` 8,000,000 less a 2,000,000 reserve), the vision ceiling (4,500,000 decoded = 6,000,000 b64 chars), `SHOT_PDF_EMBED_CAP` 9,000,000, `SHOT_B64_CAP` 16,000,000. The first two agree from independent directions at **6,000,000**. **And the lever `1118fc3` dismissed was the right one, measured against the wrong baseline**: it compared the page at 1280 and 1024, found 5,873 px both times, saw 20% and concluded scaling could not work. Height being invariant is the important half — bytes then fall linearly with width — but we do not render at 1280. Scrapfly defaults to **1920** and the call never set `resolution`. Re-measured in-browser: **1920 → 5,991 px · 1280 → 5,873 · 1024 → 5,873**, a 2% change across a near-2× width range, so 1920 → 1024 is a **48%** cut. A too-large page is now re-shot WHOLE at a narrower width — twice at most, the second width computed from the FIRST refit's own bytes rather than from the same guess again — one image, one hash, nothing downstream changed shape. The width is read back off the returned image, so the arithmetic does not rest on Scrapfly's documented default, and `resolution` is sent only on the refit (a new parameter on the rung that takes EVERY capture would risk a 422 on every listing to buy nothing). A top-of-page shot is only what is left when even that fails, and it no longer arrives silent: the card states *"This photo covers the top 34% of the page"* as arithmetic, and says nothing when the numbers are absent. `test:capture-whole-page` (68 cases, offline) pins the derivation as a RELATION across all four limits — the first version pinned equality with ONE, which is fb67429's error one constant over |
| `59aa00d` (2) | **The guard against oversized vision images was blind to the only format we produce.** `vision-limits.ts` exists to refuse an image Anthropic will reject, and its own header names a 17,729 px-tall capitalchev.ca capture as the case that *"fails outright"*. But dimensions were read from PNG's IHDR only; `pngDimensions` returns null for a JPEG, and the verdict then fell through to *"dimensions unreadable, non-PNG"* and answered **ok:true on the byte ceiling alone**. Scrapfly returns JPEG by default. A tall page compresses well — far under 4.5 MB and still 17,729 px on the long edge, sailing past the 8,000 px check into the HTTP 400 the whole module was written to prevent | green signal, no check — the guard reported "within limits" for the exact image it was built to stop | `jpegDimensions` walks the JPEG marker chain to the SOF frame header, skipping DHT/JPG/DAC which share the `0xC0–0xCF` range and would otherwise be misread as a frame header, bounded to a 256 KB prefix so a truncated or hostile file costs O(1). It is also what makes the refit above arithmetic rather than a guess: a capture too big to carry still tells us how tall the page is. **Closing it opened a regression, caught by the audit below**: with the cap raised, a whole-page capture now reaches the vision rescue instead of having been degraded to a 1,080 px viewport shot, and that rung has no HTML behind it — so a refused image returned null and killed the ENTIRE rescue. The evidence photo and the vision input do not have to be the same bytes; the rescue now takes a viewport shot for the READ and leaves the photo untouched, at the same one call the old ladder spent on its degrade |
| `59aa00d` (3) | **A 13-agent audit of the fix, run before merging it, found six more defects inside it.** (a) the vision-rescue regression above; (b) the refit was handed every remaining millisecond, so a refit that hung to the deadline left nothing for the top-of-page fallback — two billed shots and NO photo, worse than the cropped photo the change exists to replace; (c) every rung guard sat below the mandatory 8 s `rendering_wait` (3 s/5 s/8 s literals), so a rung entered in that window could not return before its own abort, burning one of five concurrency slots and possibly still being billed; (d) **the PDF print budget was derived at the wrong width** — `capScaledH` scales by the CAPTURE's width, so a NARROWER image prints TALLER, and narrow is exactly what the refit produces on the tall pages that need the pages most; (e) `listingShot` is written unconditionally while its measurements are conditional, so a replaced capture could inherit the previous image's height and print a coverage percentage about an image those numbers do not describe; (f) the refit sealed a photograph of a genuinely different rendering and `listingShotWidthPx` was written and read nowhere, while the card said "Full-page capture" about a 1079 px render | one-surface fix / claims-must-stay-backed — and (d) is this bug's own shape, one constant over | (b) the fallback's budget is reserved BEFORE a refit may run; (c) one derived floor gates every rung; (d) `CAP_MAXP` re-derived at `CAPTURE_MIN_WIDTH`, the narrowest the ladder can emit — 13 pages, where 1920 would have said 8 and covered only 11,656 px at 1024 — and the truncation notice stopped asserting *"the attached photo file contains the complete page"* in favour of the measured fraction the render loop just computed; (e) the four capture fields now move together or not at all; (f) the card names the width whenever it is below 1920. **Two of the audit's own numbers were wrong** (it put `captureFitWidth(9_070_000, 1920)` at 899 px when the shipped function returns 1079, and claimed the truncation notice was already false on every viewport degrade when a 1,080 px shot cannot reach that branch at all) — verified against source before acting, and corrected in the commit rather than inherited. [[audit-your-own-fix-same-night]] |
| `59aa00d` (4) | **`1118fc3` corrected two surfaces and left two — and the guess it removed had two more copies.** It fixed "Full-page capture of the listing" on the report card and the PDF caption, and missed the /verify drop-zone (*"This signed report seals a full-page photo of the listing"*) and the email body (*"The full-page capture rides along as its own photo file"*). /verify is the worst place for it: the shape of the capture is carried by `listingShotKind`, which is **not signed and does not ride in a share link**, so the one page whose entire job is backing claims was asserting one it could not back. Separately, `1_500_000` survived twice more in the same file — `scrapflyRenderOnce`'s screenshot cap, and the rescue photo-lock's, which compares the OTHER way and so reads differently and was easy to miss. And that photo-lock sealed a VIEWPORT render shot with no `kind` at all, which then **outranked the dedicated whole-page capture**: `attachSealedScreenshot` skipped whenever ANY `listingShot` was set, so on every rescue path the paid full-page capture lost to a picture of the top of the page, and raising the cap would never have reached those reports | one-surface fix — the "unfixed sibling" from [[repeat-fix-pattern]], inside the fix for a mislabelled capture | both surfaces now say only what they can prove; all three copies of the guess are gone; a fullpage capture REPLACES a viewport one and never the reverse; and `mergeRescued`'s `fillKeys` — which carried `listingShot`/`Sha256`/`At` and nothing else, stripping `listingShotKind` from the day `1118fc3` shipped it — now carries every capture field, so the measurements are wired as well as built. **The durable fix is not a third manual sweep**: `check:copy` gained a guarded rule pinning the occurrence count of "full-page capture/photo/screenshot", so adding one anywhere fails the build and forces whoever added it to re-confirm the claim is gated on evidence |
| `5fc4211` | **The dealer's Google rating waited on the review highlights, so it timed out.** A Sundance Mazda report read "Dealer reputation: NOT CHECKED" about an established Edmonton dealer with plenty of reviews. `get-dealer-sentiment` makes three network hops in series behind a Deno cold start — Places text search, Places details (which returns the RATING and REVIEW COUNT), then a Claude call that turns the reviews into highlights — and hop 3 had **no timeout at all**. The caller aborts the whole thing at 12 s, so on a cold cache the three hops routinely blew past it, the caller caught the abort, and the point rendered UNCHECKED. The rating was in hand after hop TWO | optional step, fatal failure | hop 3 is bounded twice: SKIPPED when the two Places calls have already spent the budget (starting a call we know cannot finish only guarantees the caller aborts) and ABORTED at its own ceiling once started, so the function still answers with the rating and the count. A bounded failure sets `parseFailed`, and the rule already in that file refuses to bake a transient failure into the 30-day cache. **The first budget was an exact fit** — 7 s + 5 s is precisely the caller's 12 s, which works only if the response, the JSON and the cache write after hop 3 are all free; the gate said "ok" on a `<=` comparison, so it now demands real headroom and the budget is 6 s + 4 s. A null dereference introduced in the fix (`claudeRes.status` on the aborted path) would have turned a graceful degrade into a 500 — the very shape being fixed — and is pinned by a case. `check:cache-ver` gained `get-dealer-sentiment`: a separate function, outside every pattern, yet it decides whether the reputation point reads NOT CHECKED at all. `test:reputation-budget` (17 cases) reads both constants out of the source and the caller's abort out of its |
| `69d6705` | **The dealer published a $795 Admin. Fee on the listing and the report said "NONE LISTED".** Vic, on the same Mazda: *"at least they transparent $795 fees but we miss them incredible"*. The D2C Media VDP blob carries the dealer's own itemisation in `customFeesList`, and the reader never looked at it — the fee audit keyed only on `addOns`, populated from a different path entirely. A dealer being MORE transparent than average produced a report saying they had disclosed nothing | absence read as knowledge | `_shared/d2c-vdp.js` reads `customFeesList` and `allIncentivesList` and reconciles them against `priceWithoutCustomFees` to decide whether the fees sit INSIDE the advertised price or on top of it — where the page's own arithmetic proves it, the copy says so and never implies they were added on. The point reads "ITEMIZED", and the PDF prints the dealer's own breakdown with the line that is actually theirs to ask about. `test:dealer-line-items` (31 cases). **The correction issued mid-fix**: I first told Vic we understated the price by exactly the $795. Wrong — JSON-LD's $58,805 already beat the blob's $58,010, so the price on his report was right; the miss was the fee itself |
| `1118fc3` | **A half-page screenshot was labelled "Full-page capture of the listing."** The capture ladder degrades to a viewport shot of the top of the listing when the full-page attempt is too large or fails — worth far more than no photo — but the degrade was SILENT. `shoot()` returned only `{ b64, mime }`, so no consumer could tell a whole-page capture from a top-of-page one, and every surface said the same thing, including the PDF a buyer puts in front of a dealer | claims must stay backed | the capture reports its `kind` and the copy follows the evidence: `viewport` says it is the top of the listing and points at the fine print, captured separately as text; `fullpage` keeps the existing claim, now actually backed; no kind recorded (an older report, or one rebuilt from a share link) says "Photo of the listing" and nothing more. The PDF caption prints from the VERIFIED canonical, which does not carry the kind, so it stopped asserting a shape it cannot prove. **Logged here with its sequel**: `59aa00d` the same day shows this fix reached two of four surfaces, that its "scaling will not work" measurement was taken against the wrong baseline, and that the degrade it made honest was firing on ordinary tall pages rather than rare ones |
| `01a9d02` | **We advertised one set of ten points and delivered a different set.** Two hand-maintained lists of ten, in different files, neither derived from the other: the landing page advertised "Negotiation leverage score" — the VERDICT computed FROM the points, never one of them — while "Financing APR", a point the report does deliver, was advertised nowhere. Separately, the heatmap numbered an "MSRP per trim" card as **"point 12 / 14"** — a card that verifies nothing, presented as a verification point, which forecloses the only honest reading of the product (ten is a defined core, the rest are additions). The PDF did the same. Vic's decision: *"its always good thing to over deliver ... minimum 10 points we will keep increasing ... yes add them to pdf file all 14 ... yes we advertising 10 points"* | one-surface fix / claims-must-stay-backed | `_shared/report-points.js` is now the single list and both marketing lists are corrected against it. Every surface renders TWO bands — "The 10-point verification" over exactly the ten, then "Also checked on this listing (N)" — with the ordinal read off each item's own `point` flag, so a context card cannot be announced as a point again whatever order the pool is built in. **The old gate could not have caught any of it**: `ten-point-claim-is-load-bearing` pins how many times the phrase "10-point" appears, and a regex occurrence count cannot observe an array's length, so it stayed green across every commit that grew the grid from 10 tiles to 16 — counting the CLAIM, never the thing claimed. Worse, its note said the App.jsx hits were "4 report surfaces"; not one is (all four are nav links). Inventory re-enumerated, and new `check:points` (21 cases) reads the arrays |
| `8401c99` | **The parity gate certified the scroll view green using the sidebar's own code.** `check-report-parity.mjs` asked `src.includes(anchor)` — anywhere in a 13,000-line file. Its entry for the sealed listing capture is labelled *"scroll view copy"* and matches a string that occurs exactly once in src/App.jsx: **inside ReportViews**. So the gate certified Scroll — the DEFAULT surface — off the Sidebar's text and exited 0 while Scroll rendered no capture, no source URL and no Archive link at all. Behind it, three real gaps Vic had reported: *"there is 'No evidence on heatmap' vs sidebar"* and *"scroll doesn't show screenshot evidence as well"* | green signal, no check (a gate satisfiable by another surface's code, and CITED as proof) | the gate is range-aware: each label maps to the function that renders it and the anchor must be found inside that function's byte range. It turned red immediately on exactly that entry. The evidence body is lifted to a module-level `<EvidenceCard a palette>` that derives everything from `a` and owns its own state, so Scroll mounts the same component on paper tokens; the duplicate SHA-256 re-check left in ReportViews (recomputing a full-page capture's digest on every render of a view that no longer read the result) is deleted. `heatItems` and `items` were TWO pools built from one set of ingredients — the Heatmap simply never spread `evidenceItem`/`sayItem` — so the Sidebar carried the dispute-proof evidence and the Heatmap did not. Both now come from one pool, and the Sidebar is `[verdictItem, ...heatItems]`: a surface may differ in LAYOUT, never in what the report contains |
| `501a9cd` + `4c5f39d` | **One signed report, four different answers to "how many trims does the manufacturer publish."** Scroll rendered all 17, the flipbook sliced to 10, the email payload to 12, the heatmap tile announced "17 trims" over a list none of the others matched — and none of them said it was truncating. Worse, the PDF and emailed HTML both stated *"the asking price sits above N of M published trim prices"* with M taken from the TRUNCATED list, so on a 17-trim ladder it divided by 12 and stated a proportion that was not true of the manufacturer's lineup. And the rows never said what differed between them: the catalog query is a substring match by design (dealers write "NX 350h", the catalog stores "NX Hybrid"), so the pool legitimately spans several separately-priced VEHICLES, and `model` was not even in the SELECT — the card structurally could not have explained six rows named "Luxury" at six prices. Vic: *"it went crazy with trims i wonder where is this duplicated data coming from"* | one-surface fix / present-without-creating-questions | one cap (`TRIM_ROWS_SHOWN`) shared by all five surfaces, each printing "Showing 12 of 17 published trims"; the payload carries the FULL ladder (bounded at 40) plus the true total, so the surfaces render a capped view and COUNT over everything. Each row is prefixed with the nameplate it belongs to when the pool spans more than one. **`4c5f39d` is the self-audit an hour later**: `501a9cd` shipped that nameplate label to 3 of 5 surfaces — [[report-features-all-views]] broken inside the commit whose subject was surfaces disagreeing about the same data, the "sibling nobody looked for" from [[repeat-fix-pattern]], committed in the fix for it. The label now reaches all five, pinned per-surface in the gate. The range check added in `8401c99` **rejected my first anchor on its first use** — I labelled it "scroll view" when the scroll trim card is its own component that QuoteCheckPage mounts — which is the strongest evidence that gate works |
| `b26a535` | **A forwarded report claimed an UNCONFIRMED recall was confirmed.** A `#r=` share link ships TWO representations of one report: `vp`, the complete signed canonical the "verified" banner checks, and a compact projection the page actually RENDERS from. Nothing kept the second honest against the first. `recalls.confirmed` was never carried and the decoder rebuilt the object without it — while canonicalReport reads `confirmed: a.recalls.confirmed !== false` — so an unconfirmed match forwarded as CONFIRMED: the shared copy made a FIRMER claim about the vehicle than the original, on the exact link a buyer forwards to a dealer. The detail list was also capped at six while the count was not, so a 9-recall report forwarded as "9 open recalls" showing 6 — the 2026-08-20 defect recurring on a surface that fix never reached. Six more fields were dropped outright: VIN (so [[vin-every-scan]] was false on every view of a shared link), odometer, market value, the dated capture provenance, the financing-math result, and the REASON an add-on was flagged — a flag with nothing behind it is a bare accusation | absence read as knowledge (a missing flag decoded as the confident value) | the encoder carries all of them; a legacy link with no confirmation flag decodes as NOT confirmed, so missing information lands on the cautious side of a claim about a vehicle; truncation past the bound is DECLARED. **Two gates, because one cannot see both halves**: `check:canonical` now compares the analysis fields canonicalReport READS against the fields encodeReport reads (run against main as it stood, it fails naming exactly capturedAt, financingCheck, marketValue, odometerKm, sourceUrl, vin) — and its comment states plainly what it cannot see, a field dropped from inside an object it already touches, because a gate that overstates its coverage is how the last one stayed green for weeks. `test:share-round-trip` (17 cases) covers that half by extracting the REAL encode/decode from source and round-tripping them: **13 of 17 fail against the pre-fix code**, including "got 6 details for a count of 9" |
| `38274c2` | **We told buyers to demand a dealer remove the manufacturer's freight.** Dealer platforms bundle everything above the vehicle price into ONE row with a generic caption; on the Convertus page behind LC-46A4-66F it read "Fees & Accessories $3,330". That caption describes the platform's addon SLOT, not the contents. Against Lexus Canada's own published Alberta figures the row is freight 2,205 + A/C excise 100 + AMVIC 10 + tire levy 20 + Lexus's own published dealer fee 995 = **$3,330.00 to the cent** — so 70% of it is money the dealer collects and remits for someone else. LotCheck attributed all of it to the dealer in three buyer-facing places, and the worst is the counter-script the buyer reads ALOUD to a named AMVIC licensee. Verified by running the real functions from `origin/main`: `addonsTotal = 3330`, and *"Please take off the $3,330 in dealer add-ons (Fees & Accessories) — I don't want them."* It fired whenever the model returned verdict `"flagged"` — the likely verdict for an unexplained $3,330 bundle — or returned none, because `classifyLine`'s terminal default is `"addon"`. The same caption also flowed into `totalFlaggedCost` ("$3,330 worth questioning") and two surfaces printed the sum under a heading reading "Add-ons total". **This is the same class as the $11,173 phantom markup**, and the S25 comment in analyze-listing-url already said it out loud: *"printing Toyota's own $3,078 of freight as dealer markup"* | absence read as knowledge (a caption we could not decompose read as an attribution) | a caption naming a PASS-THROUGH category and joining it to something else cannot be attributed to any payee, so it is not: normalised ONCE in `_shared/fee-caption.ts`, server-side, before `computeReconciliation` and before anything reads `totalFlaggedCost`, so `classifyLine`, the counter-script and every surface inherit one correction. Wired into BOTH analyze paths — a guard on one path only is what `614c399` fixed. The money is still SHOWN (hiding it would be its own defect); the buyer gets the ask that actually resolves it — *"Please itemise $3,330 in writing — freight, the dealer's administration fee, and government levies as separate lines"* — which asserts nothing and is safe to say to a dealer whose bundle is entirely legitimate. Deliberately narrow: "Dealer Fees", "Administration Fee" and "Protection Package" are still challengeable, and an unknown SINGLE charge still defaults to "worth questioning". `test:bundled-fee` 56 cases, incl. the ordering and a mixed report keeping a $3,330 bundle and a $1,995 protection package apart |
| `32903cd` | **The manufacturer publishes the whole fee stack and we were throwing it away.** `from_prices.<BRAND>.<PROV>.json` — already fetched three times per brand for the MSRP derivation — itemises freight (FPD), the brand's own published dealer fee (DRF), the A/C excise, the regulator fee (AMVIC/OMVIC/VSA), the levies, factory accessories, and its own SUBTOTAL. Only MSRP and PACKAGE were read. Without those lines a bundled dealer row is unattributable, which is precisely what made `38274c2` necessary | green signal, no check (a hardcoded fee catalog nobody could verify) | captured in the series loop from the payload already in hand — **no new HTTP, no migration**: `all_in_price` and `attrs` already exist and 38 rows already carry `attrs.all_in_breakdown` hand-seeded from B&P summaries, so the capture writes the SAME key names and is a drop-in replacement for the hand-seeding. **Measured, not assumed:** all six live payloads (Toyota + Lexus x AB/ON/QC, plus BC to check scope) = 204 series-year entries, 2,892 line items. FPD is per SERIES and identical across provinces; DRF is per make x PROVINCE (Lexus AB 995 / ON 999 / QC 795); TIR varies *within* a province by vehicle ($25 or $20 in AB) — which is why `fee-schedule.ts`'s hardcoded $25 is **wrong for 7 of 18 Lexus series, including the NX in the customer's report**. Two lines would have made the capture wrong and only real data revealed them: `ACCESSORIES` is a NESTED node whose amount lives in `items[]` and carries manufacturer-fitted block heaters ($398–$797 on 24 vehicles — **$16,759 of FACTORY accessories that would have looked like dealer padding**), and `INCENTIVES` is nested, NEGATIVE, excluded from SUBTOTAL and applied after tax. With both handled, parts == SUBTOTAL **to the cent on 204/204** entries on all three payment bases — and a stack that does NOT reconcile is REFUSED rather than stored, because an unproven decomposition is what becomes a false accusation. Basis is CASH: an advertised all-in price is what a buyer pays without financing, so the finance column would add PPSA they may never owe. Verified by a live dry run: `NX Hybrid / Premium` msrp 58,025 -> `all_in_price 61,357.18`, the manufacturer's own published cash SUBTOTAL, with the province stamped and `captured_from` naming the exact URL and model code. **One hazard closed along the way:** `attrs` is in CARRY_FORWARD, whose rule is "a fresh scrape always wins" — right for a scalar, wrong for a bag of keys, so the new capture would have silently deleted the hand-verified `seats` / `package_line` / `base_msrp` / `block_heater_included` keys on those 38 rows. `attrs` now deep-merges, fresh winning per KEY. `test:fee-stack` 62 cases, run against REAL payloads because the failure mode here is a published line nobody anticipated |
| `44acf25` | **The admin delivery ledger under-reported deliveries and painted a false green.** Five of the six paths that deliver a report charged the buyer while writing zero telemetry — including every cache hit, the highest-volume path of all — so the ledger's "green" meant "the paths we happened to instrument did not fail", not "deliveries succeeded". A panel built to answer *is this all working?* could not have said no. | green signal, no check | `instrumentDelivery()` wraps all SIX paths; a delivery that writes no telemetry is now impossible by construction rather than by discipline |
| `7f7b7c2` | **Every genuine signed report read "Seal broken — this copy was altered" when opened from its own email.** `report-sign.ts` signs the RAW canonical string and then gzips it for `verifyPayload`; the share-link check verified the GZIPPED bytes against a signature made over the UNCOMPRESSED canonical, so the two could never agree. The banner Vic hit is the one the product tells buyers to trust: *"we showing user instruction's that will fail"*. Proven with an ECDSA harness before and after — OLD wiring: SEAL BROKEN; NEW: valid; a legacy uncompressed payload: still valid. Same commit: dealer reputation was written into `analysis` AFTER the server signed it (so the emailed copy failed its own authenticity gate — [[report-email-signature-drift]] again, a *setter* rather than a field), the banner **failed OPEN to green** so an unverifiable or crashed verification rendered in the same reassuring teal as a verified one, and the trim card mixed powertrains. | green signal, no check | verification gunzips before verifying and still accepts a legacy uncompressed payload; the banner is TRI-STATE (verified / could not verify / altered) and never renders green without a completed successful check; `fetchDealerSentiment` is fill-only, so no post-signature setter can break the seal |
| `ba28cec` + `1374b6b` | **A gasoline Lexus NX was stored as "Hybrid", so a hybrid buyer got the gas car's MSRP.** Report LC-46A4-66F, a real paid report on a 2026 NX 350h, showed "MSRP · STARTING AT $55,080" — the GAS NX's base — while the dealer's own page stated $58,675 for that unit. Three write-side causes, one class ([[powertrain-identity-rule]]): (1) `model-identity.js` MARKERS had no pattern for the Japanese-luxury numeric-h convention, so `NX 350h` read as MARKER-FREE and matched the gas series — Lexus/Acura/Infiniti/Honda/Nissan never write the word "hybrid" in the nameplate, it is 350h / 500h / 450h+ / e:HEV / e-POWER / Sport Hybrid; (2) `inferFuel()` assigns fuel at the SERIES level and `flagAllOnePowertrain()` detected exactly this mis-tag but only `console.warn`'d, so the rows shipped anyway; (3) the dedupe key was case-sensitive, so `LUXURY` and `Luxury` became two rows for one trim. **This is byte-for-byte the defect a migration hand-fixed for the Lexus TX on 2026-08-26 and left unfixed for the NX** — the tracked follow-up named in that entry. `1374b6b` is the CACHE_VER bump the gate correctly demanded and I merged past: without it, cached reports replay the old wrong anchor. | one-surface fix (fixed for one nameplate, left for its sibling) | markers extended (`test:model-identity` 51→63, incl. *"a hybrid NX can never inherit the GAS NX row"*); the detector now REFUSES a **proven** mis-tag — proof being a powertrain-marked sibling nameplate for the same make/year, so a genuinely hybrid-only line (Sienna) is never refused — and the refusal is actually WIRED into `writeCatalogs` rather than computed and dropped; dedupe key lowered to `lower(trim)`; migration `20260827` corrects rows already stored wrong, collapsing case-duplicates **only at the same price** |
| `01465a4` | **One blocked fetch emptied the whole report: price, VIN, MSRP, trim, APR and days-on-lot vanished together.** The same NX report printed "ASKING PRICE: Not shown" and "VIN: NOT ON QUOTE" for a page whose own `vmsData` carried `asking_price 62005`, VIN `2T2GKCEZ8TC072832`, `msrp 58675`, 8.99% APR and `date_on_lot 2026-05-04` (a 115-day-old NEW car — leverage the buyer paid for and never got). **Nothing was wrong with the extractors**: run unchanged against a saved copy of that exact page they return every field. Every structured reader hung off ONE direct GET, and when Cloudflare refused it they all resolved null. Measured against that host: 57 of 67 requests returned a 5,927-byte challenge shell, then HTTP 429 to everything. We were making it worse ourselves — up to FIVE separate un-shared GETs at the same URL per scan (three from the retry loop at 1.5s/3s, one from the trade-in widget, one from days-on-lot, which re-downloaded the entire 1.1 MB page to regex ONE field out of the blob the main reader had already parsed) on top of Nimble and Scrapfly. | no single point of failure (a whole card's worth of facts behind one request) | new `pageHtml`: the rescue render — **already paid for on exactly these scans**, and until now parsed only by a separate later pipeline — becomes the second source for the SAME readers, direct read still winning when it succeeds. Both extra consumers take the scan's shared read. Backoff widened to 2s/8s with jitter, and an HTTP error a retry cannot fix is no longer retried. Resolution lives in `_shared/page-source.js` so `test:page-read` (19 cases) exercises the REAL function, and it **refuses any new un-shared fetch of the listing URL** — the cost of one is invisible until a report loses its price in production |
| `f264cb2` | **An NX 350h Premium was anchored to the "Executive" row — $70,878 against a car asking $62,005, and $12,853 above its true $58,025 MSRP.** Two independent causes. (1) The catalog named the BASE package by the manufacturer's internal series grade: on `NXH/2026-bkcezc` the base package is `isBase: true, name: "Premium"` while `grade` is `"LUXURY"`, so the NX 350h **Premium** was stored as trim `"LUXURY"` with the genuinely different Luxury package ($62,165) beside it — a listing saying "Premium" could never match its own row. (2) `"premium"` is a KEY_TOKEN, so every correctly-named row took the −5 grade-conflict penalty, while `"Executive"` — a real Lexus grade simply missing from KEY_TOKENS — had no key tokens at all, **escaped the penalty entirely and won at score 0**. Reproduced against the real stored ladder before any change: `{"msrp":70878,"trim":"Executive"}`. | green signal, no check (an unrecognised name scored as neutral rather than unknown) | every package is now named by its PUBLISHED name, `grade` only as the fallback it was always meant to be — **no migration**, because the daily refresh supersedes these rows and hand-writing another one-nameplate SQL fix is the very repeat this file keeps recording. Fixed as a class: naming an *unrecognised* grade now costs −3 (less than a recognised conflict, because the weaker evidence is OUR vocabulary gap), while rows making no grade claim at all are untouched. A trim name appearing twice at materially different prices with nothing to tell the rows apart can never be labelled `exact` — Ford's Mach-E, which legitimately carries "Premium" at RWD $49,990 and AWD $56,990, is distinguished by the drivetrain column and still returns exact. `test:trim` 39→44 |
| `fe57ad4` | **A multi-agent audit of THREE fixes shipped hours earlier found 22 confirmed defects in them — including the exact bug classes two of those fixes had just closed.** 32 candidates, each adversarially re-verified against real code; 10 refuted and dropped. (1) **Ordering:** the structured-data gap-fill — the only thing that recovers a gated D2C price — ran AFTER `priceVerified` and AFTER `computeLeverageScore`, so the recovered price arrived too late to count: `priceVerified` was computed off an empty `quotedPriceSource` and stuck false, and the leverage panel printed "No pricing red flags" on a report that then displayed a large over-MSRP gap. Same ordering-vs-derived-value class `63fa164` had fixed hours earlier for `allInPricing`. (2) **Provenance:** the gap-fill copied the number but not its source — `fillFromD2cVdp` is not even imported on that path (grep: zero hits) — so `quotedPriceSource` was never `"d2c_vdp"` and `priceVerified` could never be true. Identical to the Convertus bug fixed the same night. (3) **report-features-all-views:** the gated-price disclosure shipped to 2 of 7 surfaces, missing the DEFAULT Scroll view, the Book view, `/verify`, the share link, and the emailed HTML body — while the PDF stapled to that same email carried it. (4) **Unbacked claims:** the note asserted every gated D2C price is "independently published to Google's vehicle ads" (a NEW-vehicle requirement, asserted on used/CPO units too); the Book view hardcoded "the dealer's all-in price" with no `allInPricing` check; `SUMMARY_MATCHES_PRICE` missed the phrasings a "Call for pricing" page produces, so prose could tell a buyer to phone for a price printed two inches above. (5) **Scroll** was the only surface printing a bare over/under-MSRP without honouring `priceVerified`; the leverage note called an all-in figure "MSRP" while cards used ex-freight — one page, two dollar figures, neither labelled with its basis | green signal, no check / one-surface fix | gap-fill moved ABOVE both consumers, with leverage/financing/counter-script recomputed when it supplies a price; the merge now carries `quotedPriceSource` with whichever price won; disclosure hoisted to ONE module-level `gatedPriceNote()` every surface calls, added to the share round-trip, and sealed into the signed canonical (v5, both copies, `check:canonical` confirms parity) so `/verify` shows it tamper-evident. Google-ads corroboration now gated on `isNew`. **Three gates that should have caught these had holes and were fixed too:** `check:parity` had no anchor for the note (now pins all 10 surfaces individually, 44 wired — its passing independently confirms the disclosure reaches every view); `check:copy` never scanned `email-quote-report`, the artifact buyers FORWARD TO THE DEALER (added; its two condition-bound claims re-confirmed against the real conditions before their pinned counts moved — `flywheel_capture_enabled` is seeded false with no migration setting it true); and **`check:cache-ver` was a GUARANTEED NO-OP on every push to main** — after a merge HEAD *is* origin/main, so merge-base returned HEAD, the diff was empty, and it exited 0 having inspected nothing, on the exact event where a missed bump serves stale reports (now falls back to `HEAD~1` on a push, and qualifies a bare `GITHUB_BASE_REF`). 4 new `test:d2c-vdp` cases (15/15); full offline sweep green |
| `614c399` | **The UPLOAD path could accuse a named dealer off a wrong-powertrain MSRP.** `lookupVerifiedMsrp` granted `matchType: "exact"` — the label that authorises an over/under-MSRP accusation downstream — from a raw `ilike("trim", trim)` filtered on year/make/model and NOTHING else: no `fuel_type`, no `drivetrain`, `limit(1)` with no ordering. An uploaded "RAV4 XSE" quote therefore binds to whichever of the hybrid row ($50,900) or the plug-in row ($56,400) the database returns first, and the report can state an accusation built on a $5,500 powertrain mix-up. Same shape as the IONIQ 9 incident: a match-quality guard that landed on `analyze-listing-url` and never here. The file's own comment claimed "basis is the whole safeguard: only a genuine exact trim match may displace the dealer's number" — true of the resolver it feeds, false of the query feeding it. Separately, `analyze-quote` never set `original_when_new`, so an uploaded quote for a USED car ran the full new-car comparison and the leverage line added in `63fa164` printed an MSRP-gap finding against a car whose MSRP stopped being the relevant number years ago | one-surface fix (a guard that landed on one path only) | the upload path now runs the SAME `pickTrimMsrp` scorer the listing path has always used — powertrain filter, drivetrain scoring, `rowConfirmsConfig` (the winning row must actually pin the configuration the quote claims before "exact" is granted) and the `priceImplausible` ceiling. One matcher, both paths. Used vehicles get `original_when_new` on the same condition and thresholds as the listing path, with `originalMsrp` projected so surfaces render "what it cost new" instead of a comparison. 7 new regression cases (`test:quote-msrp` 24/24) pin both directions: a PHEV quote must bind to the PHEV row, and a used quote must not be able to narrate an MSRP gap. **The repaired `check:cache-ver` immediately caught this PR's own missing CACHE_VER bump** — the gate working, first try |

---

## 2026-08-26

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `e277ef8` | **A 2026 Lexus TX 350 Luxury report anchored MSRP to $81,484 and called it "Lexus's base price."** The real base is $69,855 ex-freight / $73,206.18 all-in, so the $73,862 asking was a fair ~$656 over — but the report read as if the dealer had marked the car down from a much higher "list." The dealer inflated nothing; our own catalog was wrong. Root cause: the TCI scraper (`tci-stack.mjs` `inferFuel`) tags fuel at the **series** level, so the multi-powertrain TX line (gas TX 350 + hybrid TX 500h) stored **every gas TX 350 trim as "Hybrid,"** and named the base by Lexus's internal grade **"Premium"** instead of the Canadian trim **"Luxury."** A "TX 350 Luxury" listing then matched no catalog row and resolved onto $81,484 (the F SPORT 3 row); the real TX 500h hybrids were missing entirely. A daily `replaceRows()` refresh reproduces the corruption, so a migration alone would be wiped within a day | absence read as knowledge (a wrong-trim fallback rendered as "the base price") | **guard + hand-seed** (the two-step's first half): `scripts/lib/tci-overrides.mjs` — a hand-maintained override applied to the scrape output **before write**, so every refresh reproduces the correct TX (6 gas TX 350 + 4 hybrid TX 500h, cross-province-verified MSRPs + B&P all-in). `flagAllOnePowertrain()` warns whenever any other multi-powertrain line comes back all one non-gas fuel, so the next series-tag mis-label is loud, not silent. Migration `20260826_lexus_tx_fuel_trim_fix.sql` corrected the live rows immediately (verified via `fn_catalog_msrp`: TX 350 Luxury now returns Gas $69,855 / $73,206.18 all-in). `test:tci-overrides` 11/11, wired into `gates.yml`. ~~**Tracked follow-up:** the proper root fix — per-**model** fuel in `inferFuel` + grade→trim reconciliation across all Toyota/Lexus data, tested against the live feed~~ — **CLOSED 2026-08-27 by `ba28cec` + `f264cb2`**, but only after the identical defect reached a paying customer on the NX. The override + warn was the first half of a deliberate two-step and the second half was never taken, so the guard that "makes the next mis-label loud" warned into a log nobody read while the rows shipped. **A warning is not a guard.** |

## 2026-08-25

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `45e2424` | **An adversarial edge-case sweep of the value-band gauge (`MarketBandGauge`) surfaced six defects, the flagship being a misleading default state.** With no asking price entered — the normal `/crawl` state — the gauge showed an amber **"DEALER ASKING"** pill over the market **MEDIAN** value, and its aria-label read "Dealer asking $52,990", presenting the median as if a dealer had quoted it (a screen-reader user was told a false asking figure on every blank gauge). Five more: a huge asking (`1e12`) overran the centered value box; a sub-dollar delta printed "$0 above median" (the `delta===0` test ran on the raw float while the label rounds); an inverted/negative band (`low>high`, or a negative low) drew a reversed, self-contradictory arc (`aboveRange` and `belowRange` both true); the 5-comparable floor was checked BEFORE the outlier trim, so a band could render on <5 kept comps; and a sub-dollar asking (`0.01`) rendered "$0 · below the range" | absence read as knowledge | fixes all six in the ONE shared component (so `/crawl` and the in-report gauge both get them): blank/invalid now labels **"Market median"** (teal) with "type a price to compare" and a matching aria-label; the big figure scales its font by digit count (+`nowrap`) so it never spills; `delta` is rounded before the "at median" test; a `0 < low ≤ median ≤ high` validity guard renders nothing rather than a garbage arc; the 5-listing floor is re-checked AFTER the trim in the `/crawl` reducer; and the "real asking price" gate is now `ask>=1`. Per [[report-features-all-views]], the `/verify` market-value Row now also carries the same range verdict (inside band / below / above the range) the gauge shows. Verified live across blank / -1 / 0 / 0.01 / 0.9 / 1 / exact boundaries / below / above / 1e9 / 1e12; `check:undef` / `check:syntax` / `check:parity` / `check:no-emoji` + `build` clean. Model-switching was also stress-tested and confirmed correct — an earlier "stuck band" reading was a synthetic-event **test** artifact (native-setter `change` events desync React's controlled-`<select>` value tracker), not a product bug |
| `ec93c86` | **A negative / invalid asking price rendered as a real dealer price and read "inside band."** Caught live by Vic typing `-1` into the `/crawl` value-band gauge's asking-price field: the header showed **"$-1"**, the subtext **"$40,391 below median · inside band,"** and the needle pinned to the low end — a confident, reassuring classification of a nonsense number. Root cause: `MarketBandGauge` used **two different notions of "has an asking price."** The big number, needle, delta and subtext gated on `ask` (truthy — so `-1` counts and renders), while the below/above-band classification gated on `ask>0` (so `-1` fails BOTH `aboveRange` and `belowRange` and falls through to the `"inside band"` default). The same split meant any genuine **below-lowest-listing** asking price (e.g. $25,000 on a RAV4 Hybrid banded $31,900–$48,998) would ALSO mis-read as "inside band" — a false all-clear aimed at exactly the buyer holding a below-market number | absence read as knowledge | one consistent gate — `hasAsk = Number.isFinite(ask) && ask>0` — now drives **every** branch of the shared component (domain, needle, big number, delta, aria-label, and the below/inside/above classification). Any non-positive/non-finite input (blank, 0, negative, NaN) behaves exactly like an empty field: shows the market median, makes no asking claim. A real below-low price now correctly reads **"below the range"** (teal, never alarmed), inside reads "inside band," above-high reads "above the range." The identical latent `ask?`-vs-`>0` split in the `/verify` compact market-value Row (fed by the SIGNED report's asking, so `-1` can't occur there in practice, but the class is the same) was hardened with the same `hasAsk` gate — fix-the-class, not the instance. Verified live on the dev server across all six input classes (blank / -1 / 0 / below-low / inside / above-high): each classifies correctly, and `-1` now reads plain "market median." `check:undef` / `check:syntax` / `check:parity` + `build` clean. Because `MarketBandGauge` is the one shared component, the same fix lands on both the `/crawl` gauge and the in-report gauge at once |

## 2026-08-22

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `e80122c` | **A report kept showing "Not shown" / "we couldn't read an asking price" on a listing publicly advertised at $85,995 (visible in a Google Shopping ad Vic screenshotted), on a page his own memory record already diagnosed a WEEK earlier.** Fetching the real URL directly showed the dealer runs D2C Media, not Convertus — a completely different vendor whose `window.__vdpJSON` blob templates a "Call for pricing" lead-capture message over every price field it *renders*, while `priceWithoutCustomFees` keeps the real, current ask the whole time (also independently published to Google Vehicle Ads and sitting in plain hidden `<input>` fields used for CarProof/CarGurus/GA4 — three corroborating sources for a number the page itself declines to show a visitor). The `gated-price-recovery` memory (2026-08-15, same VIN `JTM7ERAV1TD018440`) had already specified the exact fix needed (`_shared/d2c-vdp.js`) and already once caught a prior false "BUILT" claim for it — it was never actually built until this commit | one-surface fix (a whole platform, never covered) | built `_shared/d2c-vdp.js` (`extractD2cVdpVehicle`/`fillFromD2cVdp`), mirroring `convertus-vms.js`'s established contract, wired into `analyze-listing-url`'s early promise + `earlyStructuredFacts` (now a 3-way JSON-LD/Convertus/D2C merge) + its post-Claude gap-fill, and `scrapfly.ts`'s vision-rescue merge. Both the PDF and on-screen report now also carry a new, verifiable note when a price is recovered this way — naming the "Call for pricing" tactic with proof (the page's own data, corroborated by Google's ad feed), never accusing on ambiguity. D2C publishes no MSRP anywhere (confirmed against a non-gated unit on the same site in the memory) — deliberately never populated from this source. Composes with the same day's leverage-score fix: with `quotedPrice` now actually populated for this listing, the ceiling-claim logic can finally fire instead of silently no-op'ing on a missing price. 11 new regression cases in `test-d2c-vdp.mjs`, wired into `gates.yml` as its own CI step so this class can't silently regress again. Verified the extractor directly against the real fetched page (curl'd the exact URL) before wiring anything in — matches the real VIN, stock number, and $85,995 price exactly. Not verified against a fresh paid live re-scan (real Scrapfly/Claude cost) — verified by hand-tracing the fix through the full merge chain against the real extracted data instead |
| `56260db` | **Not a bug fix — a missing safeguard.** Vic, demoing LotCheck at work: every pasted URL came back a bare "try again," with no way to tell whether the paste itself was the problem. Root cause of the message itself: the client's own fetch to LotCheck's endpoint throwing (network/DNS/CORS, or a non-JSON response) falls through to one generic catch-all with no diagnostic value — could be a short-pasted link, could be a work VPN/firewall, and the copy gave no way to tell which | — | two changes: (1) three cheap, client-side, non-blocking signals fire inline the instant a URL is typed/pasted, before Analyze is ever clicked — domain-only paste, a path ending on a bare separator, or a final word-chunk that's a real prefix of a common vehicle-listing term (a curated dictionary — catches the exact real cutoff from earlier the same session, `...RAV4_Plug_In_Hybri`, one letter short of "Hybrid," which the separator check alone can't see). None block submission — verified live against a domain-only paste, the real cutoff URL, and a complete real dealer URL (no false positive). (2) the generic fallback message itself now names both real possibilities instead of a bare "try again" |


| `63fa164` | **The RAV4 PHEV report's leverage panel said "no pricing red flags" against a real ~$28,495 asking-vs-MSRP gap.** Follow-up to the 08-21 Okotoks Toyota RAV4 PHEV GR Sport AWD report below: `priceImplausible()` in `trim-match.js` correctly downgraded the MSRP match from `"exact"` to `"starting_at"` (a single catalog row can't rule out a missing higher trim — the IONIQ 9 false-accusation case) — but `computeLeverageScore` (both `analyze-listing-url` and `analyze-quote`) only ever narrates when `basis === "exact"`, so a real, large gap produced total silence on the leverage panel AND the "Price vs MSRP" explainer. The accusation-safety gate itself was correct and untouched; nothing after it knew what to do with a declined claim | green signal, no check | two-tier fix, both non-accusatory (never state markup as fact): (1) `qualifyCeilingClaim` (already built and tested in `msrp-claim.ts`, already wired into App.jsx's floor display, but its `exceeds`/`over` fields had never been consulted anywhere) asks a different question with no "missing trim" escape hatch — is the asking price above the entire lineup's most expensive real trim? For Okotoks: $23,581 above the 4-trim lineup's $62,414 ceiling (XSE Technology Package, all-in), exact to the dollar against an already-existing test fixture for this real incident. (2) no usable ceiling data → hedged fallback at the same >20%/$6,000 threshold as `priceImplausible()` itself, framed as "ask the dealer to itemize," never a stated fact. Wired into `qualifyMsrpClaim`'s shared refusal text (both analyze functions' PDF/email explainer already call it) plus App.jsx's on-screen MSRP card, which had its own separate hardcoded "no claim made" copy that would otherwise have kept contradicting the fixed leverage panel two inches away. Same pass also fixed `computeLeverageScore` running BEFORE `analysis.allInPricing`/`msrpAllIn` were resolved in both analyze functions, so its delta always compared an AMVIC all-in asking price against the ex-freight MSRP — inventing ~$3,000 of phantom markup (the "Charlesglen $11,173" class of error). 4 new regression cases in `msrp-claim.test.ts`, 48/48 passing. Not independently verified against a fresh paid live re-scan of the real Okotoks URL (that costs real Scrapfly/Claude spend) — verified instead by hand-tracing the fix against the actual seeded production catalog data (`all_in_price` values confirmed via the real migrations) and the full existing regression suite (msrp-claim 48, msrp-authority 9, trim-match 39, quote-msrp-authority 17, deal 12, verification-checkpoints 63, all green) |

## 2026-08-21

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `9420b31` | **Four real issues from two live reports Vic sent, one report each.** (1) albertahonda.com Civic Sedan LX CVT: "No asking price could be read" while the page's own Convertus `vmsData` blob carried the real $31,595 the whole time — `analyze-listing-url/index.ts`'s own inline gap-fill loop never included `"quotedPrice"` in its field list, so a correctly-extracted price was never written to the report. (2) Okotoks Toyota RAV4 PHEV GR Sport AWD: the report's $57,500 MSRP and its own "verify this MSRP" link to Toyota's $60,578 Build & Price page read as a mismatch — both numbers were correct (ex-freight vs all-in for the SAME trim, the all-in figure already hand-verified in a prior migration), nothing explained why they'd differ. (3) same report: the sealed screenshot showed Specifications/Key Features, not the price/VIN/vehicle photo at the top — the viewport-degrade capture path (used when the fullpage shot is too large; Okotoks Toyota is a dealer already named in the code as exactly that case) sent `auto_scroll=true` unconditionally, scrolling the page away from the top before the fallback screenshot fired. (4) same report: a sealed-capture URL ran off the printable PDF page edge with no ellipsis — that one line was never wrapped, unlike its sibling lines | one-surface fix (×2) / green signal, no check (×2) | (1) explicit `quotedPrice`/`quotedPriceSource` fill block added, mirroring the file's own financing/pricingDisclaimer blocks; same gap closed in `fillFromConvertusVms` (never tagged the source) and `mergeRescued` (dropped the tag even when present) — 2 new regression cases in `test-convertus-vms.mjs`, 16/16 passing. (2) one line added on both the PDF and on-screen MSRP card, using the already-computed `analysis.msrpAllIn` to explain the all-in/ex-freight gap — never re-derived. (3) `auto_scroll` now only applies to the fullpage capture, not the viewport degrade, which exists specifically to show the top of the page. (4) that line now goes through the same `wrap()` helper every other long line in the PDF already uses. **Left deliberately unfixed, pending Vic's judgment:** the same RAV4 report showed "no pricing red flags" despite a real ~$28,495 asking-vs-MSRP gap — traced to `priceImplausible()` in `trim-match.js`, a deliberate ceiling that downgrades a confident MSRP match to avoid a false accusation when a catalog might be missing a higher trim (the IONIQ 9 case it exists for); here the catalog is complete and the gap looks real, but code alone can't tell those two cases apart, and this is the exact gate that already prevented one false accusation — not touched without his sign-off. Verified against the real live pages (fetched both listings directly, ran the extractor/reproduced the size math by hand) rather than guessed |

---

## 2026-08-20

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `b080ee4` | **Nothing** — follow-up polish, not a bug fix. The repeat-multivehicle cooldown (row below) told a rejected buyer "try again in 2h/24h" with no indication of when that was, so they'd have to guess or manually retry | — | new `CountdownTimer` component on the error card: live "Try again in Xh Ym / Xm Ys / Xs" ticking off the `cooldownUntil` timestamp the server already returns, calling `onExpire` once at zero. Pure display layer, zero server changes. Verified: tick/format math checked standalone (2h90s→"2h 1m", 125s→"2m 5s", 45s→"45s", 500ms→"1s" via ceil), `check:undef` + `npm run build` clean, dev server rendered with no new console errors. Could not exercise the live network path — `gateAttempt()` requires real sign-in before the fetch fires, and faking auth to force it would cross the auth boundary — so this one is code-verified rather than live-verified, unlike the rest of this table |
| `7869997` | **Two real cost gaps in the multi-vehicle-rejection path, caught live by Vic uploading an inventory-grid screenshot.** (1) The URL-scan path (`analyze-listing-url`) had NO multi-vehicle detection at all — a pasted link to an inventory/search page, unlike an upload, had no refuse-and-don't-charge safeguard. (2) Even a correctly-uncharged rejection still spends real vendor money per attempt, and nothing capped how many times a signed-in caller could resubmit the SAME input — Vic: "it going to cost us money" | one-surface fix / green signal, no check | (1) new deterministic check in `analyze-listing-url`: counts distinct checksum-valid VINs in the page text before the Claude call, rejects at >1, no charge. (2) new migration `20260820_scan_attempt_throttle.sql` — a shared repeat-attempt throttle on both scan paths, checked before ANY vendor spend, 2nd hit on the same (identity, input) pair → 2h cooldown, 3rd+ → 24h. Client fixes on both fetch handlers for the new response codes (a real double-`res.json()` bug was caught and fixed before it could ship). Verified end-to-end against production: RPCs tested live, VIN-detection logic tested locally (2 distinct valid VINs → detected; same VIN twice → correctly not a false positive) |
| `5efdd3c` | **Comparable listings' first color pass rendered "cheapest" as cyan instead of green.** Reused `C.teal`/`C.tealBg`/`C.tealInk` for the traffic-light coloring Vic asked for (green=cheapest, amber=middle, red=priciest, matching the Days on lot card), not realizing this component's dark palette repurposes "teal" as the brand's bright-cyan accent (`#3ae0ff`) rather than a green "good" signal. Caught live verifying the fix itself, before shipping | one-surface fix | dedicated explicit green (`#10b981`) for this rank coloring instead of reusing a token whose meaning varies by theme context; `C.coral` (red) was already consistent across every palette variant so didn't need the same treatment. Also added upload guidance (whole page + accepted file formats) to the "something went wrong" error card, per the same live-site review |
| `a6ba520` | **Comparable listings (#269) never rendered on the Scroll view** — the default view after every scan. `ReportViews` (sidebar/heatmap) and `ReportFlipbook` both got the new card; Scroll turned out to be a fully separate component with its own independent `useTrimRange()` call, not reading the shared `items` array. Caught when Vic checked the live site after the migration deployed | one-surface fix | new `ComparableListingsCard`, mirroring the existing `TrimMsrpRange` component's pattern, rendered in Scroll view. Verified live with real production data |
| `bb15acd` | **The counter-script told a buyer with an already-expired factory warranty it was "plenty for now."** The "I'll pass on the extended warranty" move fired unconditionally whenever a dealer offered one, regardless of whether factory coverage was still active on THAT vehicle. Caught live: a 2022 RAV4 at 106,000 km — Toyota's real basic (3yr/60,000km) and powertrain (5yr/100,000km) coverage would both already be expired, so the reflexive "pass on it" is bad advice aimed at exactly the buyer who might most want to consider the extended plan | one-surface fix | gated on `analysis.remainingWarranty` (already computed, already shown elsewhere in the UI, just not threaded into this script before) — confirmed-expired gets a different line (compare cost vs. likely repairs, not an automatic pass); any coverage still active, or status unknown, keeps the original framing. 3 new `deal.test.ts` cases, 12/12 pass |
| `0c8342f` | **"MSRP per trim" padded a 14-trim list out to 21 with exact duplicate rows.** LE, XLE, XLE Premium and Woodland each printed twice at the identical price, back to back — a catalog refresh leaving true duplicate inserts instead of an upsert, surfaced directly in the report. Caught live on a 2026 Toyota RAV4 | green signal, no check | dedupe by (trim text, msrp) in the one shared hook every surface reads from — deliberately conservative, only collapses EXACT duplicates; two rows genuinely disagreeing on price for a similar name (same report: "LIMITED" $52,000 vs "Limited" $52,350) stay separate rather than guessing which is right. Verified live against the real production catalog: 21 rows → 14 |
| `61dc293` | **The emailed report's 2 attachments had no clue what either one was.** `LotCheck-{vehicle}.pdf` and `LotCheck-{vehicle}-listing-capture.jpg` gave no indication in Gmail's attachment strip why there were two files or what each did — read as confusing duplication. Caught live from Vic's own inbox screenshot | — | renamed to `-Report.pdf` / `-Photo-Proof.{ext}`. Considered and explicitly rejected: replacing the cryptographic seal with a visible logo watermark — a stamp is trivially reproducible on any screenshot in an image editor and proves nothing, where the whole point is that a dealer can't dispute a report by claiming LotCheck never said it. Kept the crypto; fixed the naming, which was the actual complaint |
| `dfa00f7` | see the "Closed" note under `a77052f` in Open, below — reserving vision-rescue a minimum budget instead of whatever `enrichAnalysis` left over | one-surface fix | — |
| `a77052f` | **A real, page-stated APR was hidden as "Not shown" because an untrusted LLM guess got to the field first.** Both the page-text regex backstop and the Convertus VMS gap-fill guarded themselves with "only run if `analysis.financing.rate` isn't already set" — but the LLM's own unconfirmed read fills that exact field first, with no evidenced source. Once it did, the deterministic extractors were permanently blocked from ever running on the SAME page, even where they'd find the identical number with real proof behind it. Caught live on legacyautogroup.ca (2026 Ford Explorer): the page's own visible text plainly states "5.49% financing for 84 months ... @ 5.49% APR O.A.C." — `extractAdvertisedApr` finds it correctly given that exact text (verified directly) — but the report showed "Financing APR: Not shown" | one-surface fix | guard extracted into one testable helper, `hasTrustedFinanceRate()` in `_shared/deal.ts` (alongside the `TRUSTED_APR_SOURCES` set it now exports) — a backstop runs whenever there is no TRUSTED rate yet, regardless of whether an unproven guess already sits in the field. Both backstops now share it |
| `18d31e1` | **Read Aloud always started at the verdict, ignoring what was on screen.** The speech queue was built from `P` (the raw 10-point array) starting at index 0 every time, with no awareness of `sel` (the sidebar view's current selection). Caught live: viewing "Transport Canada recalls," pressing Read Aloud narrated the verdict instead | one-surface fix | now reads from `items` (the same array the sidebar/heatmap nav lists from), starting at whatever point is on screen and wrapping from there; each utterance's `onstart` advances `sel` + a new `speakingIdx` state so the visible card and sidebar nav both highlight amber in sync with what's actually being read. Verified live: selecting recalls then Read Aloud starts with "Transport Canada recalls..." and the recalls nav button's computed styles confirm the amber highlight |
| `8cf5a10` | **The recalls display cap `6489128` supposedly removed was still there.** `r.items.slice(0, 4)` was live in the "Transport Canada recalls" report point right up to this commit — a "9 open recalls" header still delivered exactly 4 full recall bodies, the identical symptom from the day before, caught live a second time by Vic on the same easytermauto.ca report. The 2026-08-19 fix evidently landed on a different recalls rendering surface (the compact share-link encoder's own separate `.slice(0, 6)`, untouched) and missed this one — the two were never the same code | one-surface fix | rebuilt as one-recall-per-page pagination (Back/Next at the top, "Recall N of M") rather than re-stacking all N in one long scroll, which was Vic's separate complaint about the same card. Clamps the page index to `items.length` and shows an honest "detail didn't come back with this scan" message rather than indexing out of bounds when the detail list runs shorter than the count. Verified live against a hand-built 6-recall test payload on a local dev server: correctly reports "Recall 2 of 6" against a "9 open recalls" header rather than silently claiming 9 |
| `6759cf2` | **`main` itself has been red since PR #263 merged, unnoticed.** `set-scrapfly-key.mjs` (#263, "paste the Scrapfly key, have it checked before it is stored") fetches `api.scrapfly.io` with no `User-Agent`, tripping `check:jobs` exactly the way it was built to catch — but the push run for #263 (`32339315762`) failed and nobody looked, so `main`'s own CI sat red through this whole session's work on a separate branch. Found only because merging `main`'s latest into `feat/goauto-reader` (to prepare a PR back) pulled the offending file in and re-ran the gate locally | green signal, no check | one header, matching `backfill-drivetrain.mjs`'s established convention for an admin script calling a real API on our own behalf (honest identification + contact URL, not browser impersonation): `"LotCheck/1.0 (Scrapfly key verification; +https://lotcheck.ca)"`. **Open, same shape as the row two below:** nothing pages anyone when a push to `main` goes red — the daily-job / one-gate-CI-does-not-run pattern, just for an ordinary push instead of a schedule |
| `a5a48c4` | **A report accused a real dealer of a 25% rate and a $23,275 markup on a page that advertises no APR at all.** Caught live testing the golden-set tooling against `easytermauto.ca` (a 2024 Ford Bronco Sport, outside the golden pool — Vic's own pick). `analysis.financeRates.dealer.apr` was trusted identically regardless of where it came from: the SM360 feed, the Convertus VMS blob, and a deterministic page-text regex (`apr-extract.js`) all carry real evidence, but the LLM's own unconfirmed read of the page fills the same field with no check — despite the extraction prompt's explicit "never guess or invent a number," which is instruction, not a structural guarantee. Re-fetching the real page and running `extractAdvertisedApr` against the actual HTML returned `null`: nothing on the page states an APR at all. Unlike `quotedPrice` (gated by `priceVerified` before any delta claim) or MSRP (gated by `msrp-claim.ts`), dealer APR had **no** provenance gate anywhere, and the gap was six render surfaces deep: the report-point-4 badge, its "what this means" explainer, the manufacturer-comparison warning box, the flipbook page, a payment-estimate tile, the counter-script line handed to the buyer to say to the dealer's face ("I see a 4.99% promo rate advertised — I'd want that, not 25%"), the emailed/PDF deck (two independent call sites), the `#r=` share-link round-trip, and the admin accuracy telemetry (logged `apr: verified` for a fabricated number). A second, independent bug compounded it: `resolveFinanceRates()` ran at the point the LLM's raw read landed, **before** the page-text backstop and the Convertus gap-fill got their turn later in the same request — so even when the deterministic, evidence-carrying extractor found nothing (as here), and even on listings where it *would* have found something real, neither could ever reach `financeRates.dealer` at all | green signal, no check | `financeRates.dealer` now carries a `source` tag (`sm360_feed` / `convertus_vms` / `page_text` = evidenced; `"llm"` or absent = not — covers the pre-fix shape too). Every display/claim site gates on it via a shared `TRUSTED_APR_SOURCES` set (duplicated client/server since App.jsx cannot import a Deno module — `_shared/deal.ts`'s `trustedDealerApr()` is the server copy) and falls through to the same states as a genuinely undisclosed rate, never a silent wrong number. The ordering bug is fixed with a re-sync step after both backstops run. 9 new `deal.test.ts` unit tests (`test:deal`) + 6 new checkpoint cases, both wired into `gates.yml` |
| `a5a48c4` | **The odometer explainer accused a buyer's own low-mileage vehicle of a specific fraud with zero evidence.** "...but confirm it against a VIN history report, since implausibly low mileage is also the classic sign of an odometer rollback" — a hedge ("also a sign of") still plants a named accusation (rollback) the report has no basis for, the same failure shape as the APR row above but pointed at the vehicle instead of the dealer. Present in both scan paths (`analyze-listing-url` and `analyze-quote` share the identical string) | — | rewritten to recommend the same VIN-check step on its own merits, with no named fraud: "Low mileage is usually a genuine selling point — a VIN history report will confirm it, which is worth doing for any low-mileage used vehicle regardless." [[no-accusation-language]] memory saved as a standing rule broader than this one instance |
| `d131778` | **`gates.yml` called `npm run test:golden` on every push, and the file it runs did not exist on origin.** `a5a48c4` added `test:golden`/`test:deal`/`golden:build`/`golden:grade` to `package.json` and wired `test:golden` into `gates.yml`, but only staged `deal.test.ts` — the grader itself (`scripts/test-golden-grader.mjs`), its shared logic (`scripts/lib/golden.mjs`), the builder, and the committed answer-key fixture it schema-validates against had been sitting untracked on local disk since earlier the same session and were never `git add`ed. Passed every local run (the files were physically present); failed the FIRST real CI run and every one after — `Error: Cannot find module '.../scripts/test-golden-grader.mjs'` — which aborted the `gates` job before `test:deal`, `check:cache-ver`, or anything later in the step list could run at all. Caught by explicitly checking the real GitHub Actions run rather than trusting the local pass | green signal, no check | committed the 11 files `test:golden` actually needs: the grader, `lib/golden.mjs`, `build-golden-set.mjs`, `grade-golden-set.mjs`, `apply-golden-verification.mjs`, `harvest-listing-urls.mjs`, `benchmark-reports.mjs`, and `scripts/fixtures/golden/{README.md,answer-keys.json,url-pool.json,verification-report.json}` — deliberately excluding the scratch output sitting in the same directories (`benchmark-run.log`, `dry-run-check.json`, root-level `tmp-benchmark-*.json`), which are run artifacts, not fixtures the gate depends on. **No test at all guards "everything a gate references is actually committed" as its own class** — worth a `git ls-files` cross-check against every `npm run` target in `gates.yml`, not fixed here |
| `523b4f5` | **The push that fixed `test:golden` broke `check:jobs` in the same CI run.** `check:jobs`'s third-party-fetch scanner is a whole-file text match — any file containing `fetch(` near a URL with no nearby "User-Agent" fails — not an AST check, so it cannot tell a real network call from a string literal. `scripts/test-edge-function-syntax.mjs` (added the commit before) embeds `fetch(url, ...)` and URLs like `https://esm.sh` and `https://tc.canada.ca` as SYNTHETIC .ts source text fed to the parser under test; the file itself makes zero real network calls | green signal, no check | documented the exemption plainly in the file (which happens to satisfy the scanner's own "User-Agent" escape hatch) rather than adding a header nothing would ever send. Caught the same way as the row above it: watching the real CI run to completion instead of stopping at "the fix I just pushed should work" |
| `7f5714b` | **Every listing-URL report was unemailable — "This report can't be verified as one of ours."** Caught live: Vic ran a fresh scan on the same `easytermauto.ca` Bronco Sport listing from earlier the same day, hit Send, got the generic forgery-rejection message on a report that had just been signed seconds before. `report-sign.ts`'s v3 bump started projecting `sourceUrl`/`capturedAt` into the SIGNED canonical (the `source` field, added so `/verify` could show provenance for the finance-contingent fix), but nothing server-side ever set `analysis.sourceUrl`/`capturedAt` on the listing-URL scan path — grepped for an assignment and found none across all six `finalizeServerSide()` call sites. Every report therefore signed `source: null`. The client (App.jsx) had, independently and for entirely unrelated display reasons (the Evidence section's "Source: …" line), always overwritten `analysis.sourceUrl`/`capturedAt` with its own `url` + `Date.now()` right after receiving the response — harmless while canonicalReport() ignored those fields, silently fatal the moment v3 started signing them. `email-quote-report` recomputes the canonical from the submitted body and checks it against a signature made over the old `null` values: a guaranteed mismatch, on every single listing-URL report, since the v3 bump shipped. This is very likely the same root cause behind the ORIGINAL "supposed to send email automatically it didn't" complaint the same URL produced earlier | one-surface fix | `attachSealedScreenshot` (`_shared/scrapfly.ts`) now stamps `sourceUrl`/`capturedAt` unconditionally, before its own early-returns and before every finalize call site — the real fetched URL and a real server timestamp, so they're true when signed. The client now only fills a gap the server left (`data.analysis?.sourceUrl \|\| url`), never overwrites an already-signed value. `CACHE_VER` bumped so already-cached broken reports replay fresh instead of staying stuck. `check:cache-ver`'s `OUTPUT_SHAPING` list didn't cover `_shared/scrapfly.ts` — the identical blind spot `recalls` hit the day before — closed the same way. 4 new regression cases in `scrapfly-render.test.ts` pin the exact mechanism; confirmed to fail without the fix (12/14) and pass with it (14/14) |
| `6489128` | **A "9 open recalls" card offering "Show 9 recall details" delivered 4.** Caught live, same testing session, screenshot evidence: every recall shown also cut off mid-word ("...doesn't restart after a sta"). Three independent caps stacked in the same pipeline: `lookupRecalls` (both scan paths) only ever fetched detail for the first **8** recall numbers via `Array.from(byNumber.keys()).slice(0, 8)`, so past 8 real recalls the extra ones were never even requested from Transport Canada though the header count (`byNumber.size`) was already correct; each fetched recall's own "Safety Risk:" text was silently truncated at **400 characters** with no ellipsis or indicator; the client (App.jsx scroll-view recalls card) then re-capped the already-incomplete `items` array to **4** for display regardless of what the count or the button's own label ("Show N recall details") promised. Three unrelated caps compounding is why the true 9→4 gap looked worse than any one of them alone | green signal, no check | server cap raised 8→20 (headroom; real vehicles don't approach it) in both scan paths, summary truncation raised to 2000 chars with an ellipsis added ONLY when a comment still exceeds it, client display cap removed entirely — every item the server returns now renders. The flip-book's fixed-layout recalls page keeps its 5-row cap (real page-space constraint, unlike the scroll view's expandable section) but now states "+ N more — see the full report" instead of dropping them silently. [[recalls-detail-list-must-match-count]] memory saved, cross-linked to the existing [[make-recalls-fail-safe]] guard (a different failure direction: that one guards a false CLEAN bill, this one guards a promised count actually being delivered) |

**Note on the recalls fix above:** its server-side half was re-targeted during rebase from `analyze-listing-url`/`analyze-quote`'s local `lookupRecalls` (deleted by `4aca3c3`/`a59c7c0` the same day) to the equivalent caps in `_shared/recalls.ts`, the new single source — see that commit's own entry above for the corruption this rebase separately found and fixed in the same three files.

| `cc31680` | **A recall-lookup consolidation (`4aca3c3`) truncated several deleted lines down to their bare first character instead of removing them, and it shipped through 47 green gates.** In `analyze-listing-url/index.ts` and `analyze-quote/index.ts`, `TC_VRDB_BASE`/`TC_RECALLS_PAGE`/`tcRecordToObj` were correctly deleted, but `tcFetchJson`'s function header was replaced with the literal text `a> {` while its real body — `const controller = new AbortController(); ...` — was left completely intact and unreachable directly below it; two more spots left bare `f` and `a` tokens sitting between real comments. In `search-recalls/index.ts` the same pattern additionally glued a stray letter onto the FRONT of the next comment line (`c// Two-word makes...`, `f// TC dates arrive...`), and its orphaned `a> {` body was worse than dead weight: that file already imports `tcFetchJson` from the new `_shared/recalls.ts`, so the mangled local copy was a second declaration of the same name. Found by accident while resolving an unrelated rebase conflict in the exact same files. `node --check` accepts a bare identifier statement as valid (if useless) JavaScript, so it parsed clean — and none of the 47 offline gates parse `supabase/functions/**/*.ts` at all, only `scripts/*.mjs` and `src/`. Deno, what Supabase Edge Functions actually runs, was not available in this environment to verify against directly, so severity (hard crash vs. silent no-op) could not be fully confirmed before the fix landed. `test:recall-source` already asserts every migrated symbol is "declared exactly once" and still missed this, because it pattern-matches on a real declaration keyword — `a> {` doesn't match `function tcFetchJson`, so the corrupted local copy was invisible to the test built specifically to catch duplicate declarations | green signal, no check | all three files' orphaned/corrupted spans deleted outright (the real, working declarations live in `_shared/recalls.ts` and are already imported everywhere they're used). Verified: `node --check` clean on all three files individually; an exhaustive sweep of every `.ts` file on the branch for the same signature (bare or comment-glued single lowercase letter) found nothing else; `test:recall-source`'s full 16 assertions pass unchanged. **Closed same day, `check:edge-syntax`:** see the row immediately below — the gap this row flagged (no `.ts` check anywhere for `supabase/functions/**/*.ts`) now has one, and the two real corrupted shapes above are exactly what its regression suite reconstructs and asserts against |
| `a0bfb2d` | **Nothing in this repo parsed `supabase/functions/**/*.ts` at all — the exact gap the row above shipped through.** `check:syntax` covers `scripts/*.mjs`, `check:undef` covers a fixed list of `src/` files, the build only compiles `src/`. A PURE parse check would only have caught HALF of the real corruption, though: Babel's parser is stricter than Node's own `node --check` and independently rejects the `a> {` shape as a genuine syntax error (confirmed: Node's `--check` accepted that exact text live, during the incident, with no complaint) — but the OTHER real shape, a stray letter glued onto the front of the next comment line (`c// Two-word makes...`, `search-recalls/index.ts`), parses cleanly under either parser, since `c` alone is an ordinary, if pointless, expression statement | green signal, no check | two independent checks, either failing is red: **parse** (Babel, catches the `a> {` shape) and **scope** (real `@babel/traverse` resolution, the identical technique `check:undef` already uses for `src/App.jsx`'s 2026-08-12 msrpExactScroll crash — catches the comment-glued shape, since `c` is referenced with no binding anywhere). `scripts/lib/edge-syntax-check.mjs` holds the pure logic; `test:edge-syntax` reconstructs BOTH real corrupted shapes byte-for-byte from `git show 717b681:...` and asserts each is caught by the correct half, plus false-positive guards for TS-only syntax (interfaces, type aliases, function-type parameter names, type-only imports, `Deno`/web-standard globals) verified clean against all 47 real files first. Wired into `gates.yml`'s `render-safety` job (needs `npm ci` for Babel, same as `check:undef`) |

## 2026-08-19

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `55509fe` | **A 401 that would not say why cost a whole 452-host rescue pass.** Every rescue failed with a bare `scrapfly HTTP 401` or `429` — enough to know it broke, not enough to know whether the key was wrong, the account throttled, or the concurrency ceiling the cause. Isolating it took a second run at `--limit 60`, where the concurrency pressure disappears and all 22 rescues still returned 401. The 429s were a side effect; the credential itself is rejected. Same defect this probe had just fixed for DEALER hosts in `91d7a09`, left in place one layer down for our own vendor | absence read as knowledge | the failing call now reports Scrapfly's own body — `Invalid API key - make sure to provide it via key query parameter` reads nothing like a throttle — and the key is `.trim()`ed, because a secret pasted into `gh secret set` keeps whatever whitespace came with it and a trailing newline makes a `%0A`-suffixed key that returns exactly the same flat 401 |
| `bd355c7` | **The backoff gate rolled dice — an ~18% flake.** `test:fetch-retry` asserted `delays[1] > delays[0]` over bases 100/200 while the implementation added `Math.floor(Math.random()*250)` inline, false whenever the first jitter draw beat the second by 100ms+. It went red on PR #249's merge-ref run minutes after passing on main. A gate that can fail on a dice roll is worse than no gate: it teaches everyone that red means re-run, which is exactly how a real red gets ignored | green signal, no check | randomness moved behind the same injectable seam `fetchImpl`/`sleep` already use; the backoff case injects zero jitter and asserts the bases EXACTLY; a new pin keeps `Math.random` out of the delay expression so the assertion cannot quietly go probabilistic again. 10 consecutive green runs, production behaviour unchanged |
| `6800f65` | **A fixture demanded that a hybrid lose its identity, and `test:recalls` was red on main from `aa6183a` until someone looked.** The fixture asserted `"RAV4 Hybrid XSE" -> "RAV4"`; `aa6183a` had deliberately added `RAV4 Hybrid` to `CANONICAL_MODELS`, because msrp_catalog keys hybrid rows under that name and the lookup matches with no wildcard — collapsing to `RAV4` both kills those rows and lets a hybrid inherit its gas sibling's MSRP, the exact blur the powertrain-identity rule exists to prevent. The resolver was right and the fixture was stale. It stayed red because `test:recalls` is the one gate `gates.yml` deliberately does not run (it queries Transport Canada live), so CI was green over it — the same shape as `e2c5886` | green signal, no check | fixture corrected to `RAV4 Hybrid` with the reason recorded beside it so it is not "fixed" back. The recall path was never affected: `modelCandidates` tries the resolved base first and falls through to shorter names, still reaching `RAV4`, which is the name TC knows — pinned end-to-end and verified live (queried `RAV4`, count 3) |
| `9b55e46` | **The daily recall sweep read all 40,471 recalls and wrote none of them.** First live run, dispatched right after the migration landed: every make read fine from Transport Canada, every single write failed with `Unexpected end of JSON input`. The helper ended `res.status === 204 ? null : res.json()`, which assumes an empty body only ever arrives as **204** — but PostgREST answers a write carrying `Prefer: return=minimal` with **201 Created and an empty body**, so `.json()` was handed `""` and threw. Both writes use `return=minimal`, the recall upsert *and* the `recall_sweep` audit insert, so nothing could be written at all — including the audit row whose job is to record the failure | green signal, no check | body read once as text, parsed only if non-empty: covers 201-with-no-body, 204 and an ordinary JSON read alike. **The gate could not have caught this**: `test:recall-sweep` stubs `fetch` and `--dry-run` skips the Supabase path entirely, so the write is the one branch a stubbed gate structurally cannot reach — I verified the whole read path against the live registry and never exercised the write against a real server. Pinned at source level (read text, parse conditionally, never `res.json()`) and mutation-tested. Checked the class: three other files use `return=minimal` and none parse that write's response — every `.json()` in them is on a `GET …select=`. **The fail-safe held throughout**: no make written, none recorded as clean, summary said the data was unchanged and NOT being reported as clean, run exited red |
| `fd7f5a2` | **Recalls were only ever checked at report time, so a recall issued the day after a buyer's report was invisible to them.** The lookup ran inside `analyze-quote` / `analyze-listing-url` and the answer was never kept, which is right for a report — it is always fresh — but it means a buyer holds a signed PDF saying "No open recalls found" that was true when signed and is silently wrong now. The buyer most likely to be harmed is the one who acted on it: bought the car, filed the report, and has no reason to re-run the check. Detecting "NEW" requires yesterday to still exist, and nothing was persisted — audited across five modalities (CI cron, Supabase pg_cron, every call site, stored state, stated intent), all five confirming zero scheduled checks; the only occurrence of "recall" in any workflow file was `gates.yml`'s exclusion notice | absence read as knowledge | daily sweep of all 35 canonical makes into `vehicle_recall`, diffed against the prior day. **Building it surfaced a worse bug than the one it fixed:** the VRDB caps a response with no count, no envelope and no `hasMore` — `/make-name/TOYOTA/year-range/2025-2025` returns 25 rows / 13 recalls, the same query with `&limit=200` returns 42 rows / 23, and the nine hidden ones include RAV4 `2026201` and HIGHLANDER `2025694`. A naive sweep would have gone green every morning on a 43% blind spot. Completeness is now something the registry must **demonstrate** — a response strictly smaller than the ask — and a fixed ceiling was proven wrong on the first live run when Ford returned exactly 5,000 rows at limit 5,000 (true size 5,167 / 1,343), so the sweep escalates 8x until the list provably ends, then refuses. `test:recall-sweep`, 36 assertions, every guard mutation-tested to fail on its own regression |
| `fd7f5a2` | **Three ways the sweep could have turned a data gap into a false all-clear.** (1) An unreadable make would have written as "0 recalls". (2) A make whose name TC does not recognise returns an empty `ResultSet` indistinguishable from a spotless marque — measured live, all 35 makes have recalls, the smallest being Polestar at 11, so zero is never a fact here. (3) The cold start found 40,471 recalls "not on file", issued over 57 years; staging those as new would be false on its face and would teach the reader the report is noise, which is how a monitor dies | absence read as knowledge | the sweep has no third state — a failure carries no `rows` array at all, so a caller cannot mistake it for an empty registry. An empty read is a failure, a >50% collapse is refused, a zero against a non-empty history says *why* ("recalls are not withdrawn in bulk") so the operator diagnoses upstream rather than reading "collapsed 100%". A first sweep records a baseline silently and only what appears after it counts. A failed make leaves a `recall_sweep` row and never overwrites its recalls; one OEM's outage cannot cost the other 34 their sweep; a partial sweep exits RED |
| `fd7f5a2` | **The job stages, and structurally cannot send.** A daily recall finding is exactly the kind of thing that wants an email, and the standing rule is that scheduled jobs compute and stage while every outbound send is approved | — | no delivery path exists in the script and `test:recall-sweep` fails if one is added (nodemailer/sendgrid/resend/postmark/mailgun/twilio/slack/webhook/`email-quote-report`). The staged summary states in words that nothing was sent. Findings are grouped by recall rather than by model-year row — one recall spans every affected year, so Ford `2026339` is one line reading "EXPLORER 2020–2026" instead of seven |
| `a59c7c0` | **The module that called itself "the SINGLE source of the recall lookup logic" was imported by exactly one file — its own test.** Both report paths carried their own `lookupRecalls`, `search-recalls` carried a third set of the primitives under a comment saying its logic was "lifted from analyze-quote", and there were four copies of `TC_VRDB_BASE`. **The copies had already drifted, and the only correct one was the dead one:** `_shared` walked the whole candidate ladder through `tcModelKnown`, while both shipped copies probed a single name (`!!baseModel \|\| await tcModelKnown(make, baseModel \|\| model, year)`), so a renamed nameplate degraded to `confirmed:false`. The regression harness proved nothing about any of it, because it exercised the one implementation that never ran. A "single source" nothing imports is indistinguishable from dead code, and its tests stay green forever | one-surface fix | one declaration of each of the seven symbols, in `_shared/recalls.ts`; both report paths and `search-recalls` reach it by import. `test:recall-source` pins all three properties — exactly one declaration, every consumer importing what it uses, and **at least two production importers**, because the duplication was not the root failure; the absent consumer was. `search-recalls` keeps its own list/detail logic on purpose: it answers a different contract (free-text query, year widening, page-shaped results), and bending it to fit `lookupRecalls` would be a worse kind of sharing |
| `a59c7c0` | **A clean bill backed by our own catalogue rather than by the registry.** `confirmed` began as `let confirmed = !!baseModel`, on the stated reasoning that "baseModel (resolved canonically) proves" TC tracks the model. It proves nothing of the sort — in production `baseModel` comes from `resolveBaseModel()`, which matches rows in **our own `msrp_catalog`** — so wherever LotCheck's catalogue and Transport Canada's naming disagree, the short-circuit skipped the only question that mattered. Measured live 2026-08-19: a 2026 Toyota 4Runner Hybrid resolves to base model "4Runner Hybrid", TC has no such nameplate (everything files under `4RUNNER`), the year's list came back empty — and the buyer was shown **"No open recalls found", confirmed**, for a name the registry never matched. Exactly what make-recalls-fail-safe exists to prevent: a lookup miss rendered as a clean bill | absence read as knowledge | nothing is assumed — every candidate is offered to TC, and the response records `confirmedBy`, the name the registry actually recognised, so the claim carries its own provenance. 4Runner Hybrid now confirms via `4Runner`, backed rather than assumed. `test:recall-source` forbids the short-circuit returning, requires the ladder probe, and checks `confirmedBy` **in the returned object** — asserting only that the identifier appeared somewhere still passed with it dropped from the return, which the mutation test caught. All 20 live fixtures unchanged |
| `8fe14dc` | **The public market read let the DEALER supply the reference, so a self-ratified sticker read as an honest one.** `fn_alberta_msrp_deviation` compared each listing's asking price against the MSRP the dealer stated on their own page — and a dealer printing MSRP = asking price is arithmetically invisible to the over-sticker count. Proven live: Southpointe Toyota Tacoma Hybrid, asking $89,130, page MSRP $89,130, trim hidden. PR #245 relabelled the card honestly as the interim step; this replaces the read. The replacement's own trap was the freight basis: once OUR catalog is the reference, an all-in advertised price sits a freight's-width above an ex-freight MSRP, and only 28 of 1,084 catalog rows hold the manufacturer's all-in figure — a point comparison would either fabricate markups or gate the read forever | absence read as knowledge | province read anchors to `msrp_catalog` (exact trim matches only) and judges each listing against a WINDOW `[msrp, msrp + ceiling]` guaranteed to contain the true all-in figure: **under** below it, **over** above it, **indeterminate** inside it — disclosed by count, never guessed into a direction. Every published % is a floor (cross-multiplication proof in the suite), so the read can understate a distance but never overstate one. The k-anonymity floor counts directional calls only — indeterminates cannot buy publication — and a stale row returns a dated pause instead of posing as current. First live run made the blind spot measurable: **199 of 1,721** exact matches ask above sticker plus the FULL freight-and-fees allowance, and **269 of 1,420** dealer-printed stickers exceed the manufacturer's ceiling for their exact trim (median floor +5.0%) — the class the old read necessarily reported as zero. `test:market-catalog` (40 cases) + 8 page pins in `test:price-index`; the dealer-stated RPC is pinned OUT of the page |
| `5fda4f8` | **"0 above sticker" was a near-tautology: the denominator was the dealer's own number.** `fn_alberta_msrp_deviation` compares each asking price against the MSRP the DEALER prints on their own listing. Southpointe Toyota's Tacoma Hybrid asked $89,130 with the page's MSRP field set to $89,130 and the trim name hidden — and counted as "at sticker". The market card presented this read as "asking price vs sticker", implying a manufacturer reference it never had | absence read as knowledge | the surface now names its denominator — title, headline sentence, strip labels and caption all say "the sticker the dealer states", plus a clarifier that the catalog-anchored comparison is coming. INTERIM by design: the structural fix (`fn_alberta_market_vs_catalog`, basis-aware, PR #249) was built the same day in a parallel session and made the blind spot measurable — 199 of 1,721 exact-matched listings ask above sticker + a full freight allowance, where the dealer-stated read necessarily reported ~0 |
| `f0ac0cb` | **One transient HTTP 504 killed a whole make's daily refresh.** Mercedes' inventory endpoint gateway-timed out once on 2026-08-19; the scraper's one-shot fetch died on it, and the fresh-write guard (#161) correctly went red — but the fetch never deserved to die: the identical call succeeds the next day. A make's daily freshness was hostage to a single retryable hiccup | optional step, fatal failure | `scripts/lib/fetch-retry.mjs`: 3 bounded attempts with backoff + jitter, retrying ONLY what a retry can fix (network throws, 429, 5xx) — a 4xx returns immediately so real answers still fail fast — and every attempt carries its own abort timeout so a hung socket cannot stall the run. Wired into `scrape-mercedes.mjs`; `test:fetch-retry` (8 cases incl. exact attempt counts and no-retry-on-404) runs in `gates.yml` |
| `2b57add` | **28% of Alberta never answered the feed probe, and we counted the other 72% as if it were the province.** 161 blocked, 227 unreachable, 59 timed out. It is not that those dealers have no feed: West Wind Honda (Lethbridge) answered `403` to all SEVEN probes from the GitHub runner and `200` from an ordinary connection, where our own `extractEdealerVehicles()` reads 15 vehicles with VINs off it. Westside Mitsubishi (Edmonton) is a second, same story. Both run EDealer — the platform the run had just reported as having ZERO sites province-wide. So a headline finding was a fact about our egress address. `crawl-inventory.yml` runs from the same place, so this caps PRODUCTION collection, not just the survey | absence read as knowledge | `--rescue` retries only hosts that never answered, through a Canadian residential exit (`asp=true`, `country=ca`) — already in the stack for page render. Cheap by construction: a host that answered directly never costs a credit, one `/new/` fetch serves all three HTML detectors, and SM360's JSON endpoint is tried only when that yields nothing — two billed calls per rescued host, not seven. Rescued hosts are marked `rescued:true`, because needing a residential exit is a standing fact about that dealer, not a detail of one run |
| `91d7a09` | **"EDealer: 0 across 1,639 Alberta hosts" was reported as a finding. It was the absence of one.** Every detector returned a bare `null`, `snapshot()` kept only the four platform arrays, and the console printed one line: `no feed detected: N`. So 1,607 of 1,639 hosts produced a single number between them, and "no dealer here runs EDealer" was indistinguishable from "every request was refused" — which is exactly what had happened | absence read as knowledge | every detector records what happened on each path it tried (HTTP status, non-JSON content-type, a 200 with no marker and its page size, timeout, DNS failure); misses ship IN the artifact with that trace instead of being dropped; the run prints a breakdown by reason AND how many hosts actually answered, since a platform count is meaningless without it. A 200 carrying `vehicleArray` that our parser reads as zero rows is flagged `PARSER BUG` explicitly — our defect used to look identical to "not an EDealer site" |
| `aa77a97` | **The AMVIC candidate list was paginated with `.range()` and no `.order()`.** Postgres gives no guaranteed row order without ORDER BY, so successive windows can overlap and leave gaps: the list could silently lose licensees and gain duplicates, and "1,639 distinct usable hosts" was neither trustworthy nor reproducible. It also made a capped run unresumable — the run that hit the 45-minute cap at 1,580 of 1,639 could not be finished, because two runs of that query need not agree on which hosts came first | green signal, no check | `.order("id")` on the paginated read; `--skip N` applied before `--limit` so a capped run resumes; job timeout 45 → 90 → 180 as the rescue pass was added. A probe that cannot finish its candidate list reports a clock answer as a coverage answer, and this one did — twice |
| `5cf6fdd` | **`city_dealer_index` was keyed on `dealer_source.city` verbatim.** That column is free text off two rosters (AMVIC's licensee list, OSM's `addr:city`), so one place arrives as "St. Albert", "ST. ALBERT", "St Albert", "Saint Albert", "Leduc, AB" — and the unique constraint is on `(city, province)`, so every spelling became its own row. Each fragment carries a share of the dealers and listings, so a well-covered city can sit below the publish gate in EVERY fragment and appear nowhere. Invisible from punctuation, and invisible in a way that reads as "no data here". Also: a dealer with no city silently dropped its entire inventory and reported one number | green signal, no check | `cityKey()` groups and `prettyCity()` labels, deliberately separate — normalising hard enough to collapse the spellings would print "Fort Mcmurray". Null-city dealers are now named with the listing count each one cost. `test:city-index` pins both directions: seven spelling groups collapse to one key, ten real Alberta cities stay distinct. Mutation-checked — restoring the raw grouping fails 8 |

## 2026-08-18

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `e2c5886` | **A gate stayed red on main for a week because it is the one gate CI does not run.** `test:recalls` queries Transport Canada live, so `gates.yml` deliberately excludes it — which means `aa6183a` broke it and nothing said so. The fixture demanded `"RAV4 Hybrid XSE" -> "RAV4"`, noted *"trim + powertrain strip"*, but `aa6183a` added `"RAV4 Hybrid"` to `CANONICAL_MODELS` **on purpose**: `msrp_catalog` keys hybrid rows under that name and the lookup matches with no wildcard, so collapsing to `"RAV4"` both kills those rows and lets a hybrid inherit its gas sibling's MSRP — a ~$5,500 gap on RAV4, and exactly the blur the powertrain-identity rule exists to prevent. The resolver was right and the assertion was stale | green signal, no check | expectation corrected to `"RAV4 Hybrid"` with the reasoning recorded inline so it is not "fixed" back. The recall path was never at risk and the live fixture proves it end-to-end: `modelCandidates` tries the resolved base first, then drops trailing words, so it still reaches `"RAV4"` — the name TC actually knows (`queried="RAV4" count=3 confirmed=true`). The tri-state contract is untouched, so no resolver change can turn a miss into a clean bill. 20/20. **Still open:** Toyota carries `"RAV4 Hybrid"`, `"Corolla Cross Hybrid"` and `"4Runner Hybrid"` but not `"Camry Hybrid"` or `"Grand Highlander Hybrid"` — same trap, flagged rather than fixed blind since adding names could break lookups keyed the other way |
| `1077572` | **Seven empty columns squeezed the seven real ones until their counts bled outside the card.** The sticker-terrain panel drew the above-sticker side as seven fixed slots, empty bands shown as dashed ghosts. Nothing in Alberta is priced above sticker, so seven of the fourteen columns were permanent placeholders: the real columns collapsed to ~33px and `.mkt-n`, absolutely positioned and centred on each bar, overflowed the card at **every** width including 1440px. **My first fix was on the wrong axis** — I gated the collapse on a `max-width` media query and raised the breakpoint from 560px to 1100px when 846px still overflowed. That treats an empty half as a small-screen problem; the half is empty because of the **data**. The breakpoint would have been wrong again at the next card width, and worse, it hid columns by viewport — so a band that *did* hold listings would have disappeared on a phone | absence read as knowledge | a band with no listings gets **no column at any width**, and the zero is named in words so the axis does not just stop at the wall. The inverse is guarded too: no `.mkt-col` may carry `display:none`, and when fourteen bands genuinely do fill, the terrain **scrolls** rather than dropping or crushing a column — real counts are never the thing that gives way. Exercised with synthetic input since live data never enters that branch: 14 columns, above summing to exactly 700, below to 900. `test:price-index` gains 9 assertions (21 total); each was mutation-tested to fail on its own regression and nothing else |
| `f3c4a28` | **`fuel_type` was wiped by every refresh, so any backfill would have been pointless.** 613 of 1,000 `msrp_catalog` rows carry no powertrain; eight makes are 100% null (Ram, Volkswagen, Acura, Lincoln, Chrysler, Infiniti, Buick, Alfa Romeo). The scrapers are **not** at fault — all 18 that write MSRP rows emit the field, and `inferFuelFromName()` correctly returns null because it only matches `hybrid` / `plug-in\|4xe\|phev\|prime` / `electrified\|ev\|ioniq\|electric\|e-tron\|recharge\|gv60` in the **trim string**. A Ram 1500, a Jetta, an MDX and a Navigator contain none of those words, so a plain gas nameplate has no signal in its own name. But `fuel_type` was missing from `CARRY_FORWARD`, and `replaceRows()` deletes every row for a make and re-inserts what the scraper produced — so a value established from an authoritative source would be destroyed by the very next daily refresh, before anyone saw it. Same mechanism that once left `msrp_catalog.drivetrain` **0/881** populated | green signal, no check | `fuel_type` added to `CARRY_FORWARD` **before** any backfill, so the backfill has somewhere to land. `test:carry-forward` caught both consequences rather than me noticing them: its coverage case demands a real test for every new column, and its LAYER 2 case warned that using `fuel_type` as the ragged-field fixture would silently become a no-op once layer 1 fills it — which would have made `uniformKeys` look like dead code. That fixture moved to `all_in_price`. 29/29 |
| `2841ade` | **A generic class name silently animated the whole powertrain panel sideways.** `39483b4` named the panel's bar `<span class="track">`; the per-make ticker defined a bare global `.track{animation:scroll 46s}` translating to `-50%`, so every rail inherited the marquee and slid. Measured on production: `getComputedStyle('.ptrow .track').animationName === "scroll"`. **I misdiagnosed it first** — the row geometry measured clean, because `getBoundingClientRect` reports the *animated* position, so a uniform translate looks correct row-by-row while the panel slides. I called it GPU paint corruption instead of checking `animationName` | one-surface fix | ticker removed (as asked) and the bar renamed `ptbar`/`ptfill`, then `ptr-` — the rename is the durable half, since in a single-file page a bare class inherits whatever else defines it. `test:price-index` pins the prefix; 2 assertions fail against `70da8dd`, the version that shipped with the collision |
| `39483b4` | **The powertrain panel spent 64% of its width saying "we don't know."** The largest segment of the stacked bar was `Unrecorded` — 695 of 1,084 trims — which a buyer cannot act on, and which the `recorded: 36%` chip already stated. Rescaling the coloured part was not available either: powertrain is recorded for 36% of the catalogue and that subset is skewed toward electrified, so a 100%-stacked bar drawn from it invites "half the market is hybrid" — the same inversion the per-powertrain medians are withheld to avoid | absence read as knowledge | geometry changed rather than scaling: independent rows share no whole, so no share can be read off them. Fixed powertrain order, never sorted by count (a leaderboard of our coverage reads as a ranking of what is common on the road). The 695 stated once, in words, with "not a representative sample". `test:price-index` pins all three |
| `6f3609b` | **The city-price-index rebuild step silently never ran.** I added `if: ${{ inputs.dry_run == 'false' }}` to `crawl-inventory.yml` to run the aggregation only after a real crawl. `inputs.dry_run` is a `type: boolean` workflow input, and GitHub Actions' expression comparison converts the string `'false'` to `NaN` when compared against a boolean's numeric form — the equality can never be true. Run `32097301467` crawled 9,571 units and the job reported ✅, but "Rebuild city price index" showed **skipped**, not run, and nothing caught it because a skipped step is not a failed one | green signal, no check | condition changed to `if: ${{ !inputs.dry_run }}`. Verified by re-dispatching (`32098012785`) and reading the step's own console output, not just its checkmark: 31 dealers, 6,672 listings, 2,115 matched, `city_dealer_index` written with real Edmonton/Calgary rows |
| `fcfcf23` | **A real "Contact Us For Price" gate went undetected.** Fish Creek Nissan's Rock Creek listing price-gates in plain sight (screenshot-confirmed), but the report said "no 'contact for price' call-to-action either" — the sealed capture's own screenshot came back incomplete (the sidebar carrying the CTA never rendered), so vision never saw it, and the accusation-gate correctly refused to accuse on an incomplete read. Refusing to accuse on ambiguity is right; what it produced instead was a false CLEAN reading, not an honest "couldn't verify" | absence read as knowledge | `PRICE_GATE_CTA_RE` in `scrapfly.ts` matches the raw rendered DOM text directly, independent of whether the screenshot capture was complete — wired into all four `rescueListingViaScrapfly` call sites in `analyze-listing-url/index.ts`, including a render-only last-resort path that previously had no way to surface a confirmed gate at all. First landed in `20bb781` (PR #159, merged `333f320`) and was overwritten by the concurrent screenshot-rescue rewrite (`b346612` and after) landing on the same lines — redone against the current structure and re-verified. `check:scan-accuracy` guards both the check's existence and that all four sites consume it |
| `fcfcf23` | **Blank sealed-evidence screenshots on sites with a blocking cookie-consent overlay.** Taza Park Volkswagen's "Consent Management" dialog sits in front of the page until a human clicks through it — a bot render never does, so the capture photographed the overlay's backdrop instead of the listing. The capture call itself reported success (real bytes, no HTTP error): a green signal with nothing behind it | green signal, no check | both Scrapfly screenshot calls (`scrapflyRenderOnce`'s vision-analysis shot and `captureListingScreenshot`'s sealed-evidence shot) inject `DISMISS_OVERLAYS_JS` — strips fixed/sticky high-z-index elements before shooting, generic across CMP vendors rather than chasing individual button selectors. Same landed-then-lost history as the row above; `check:scan-accuracy` now pins both call sites |

## 2026-08-17

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `a3db8f7` | **`/founders` 404'd in local dev.** `vite.config.js` keeps a dev-only mirror of `vercel.json`'s rewrite (every non-`/api/*` path -> `/app.html`), but production's rewrite is a BLANKET one while the dev mirror is an explicit allowlist — so every new route works in production and silently does not exist locally until someone remembers to add it. Production was unaffected; the route was simply untestable, which is the worse half of the trade because it is the half nobody notices until they are debugging the wrong thing | one-surface fix | `/founders` added to the mirror. The shape is worth naming: a dev config that ENUMERATES what production matches by pattern will drift every time a route is added, and drift in that direction always reads as "the feature is broken" rather than "the mirror is stale" |
| `658c25f` | **I shipped a script that could not be parsed, and every gate went green.** My `2d43c04` patch wrote a literal newline inside a template literal in `fca-stack.mjs`, so the file failed to parse at all — and the daily refresh discovered it the next morning by taking down **Jeep, Ram, Dodge and Chrysler**, which wrote nothing. The irony is exact: that patch existed to stop one make's failure costing the others, and it cost four. Nothing caught it because `build` compiles `src/` only, the gate suite *runs* about a dozen scripts but not this one, and `check:undef` parses `App.jsx` | green signal, no check | `check:syntax` parses every `.mjs`/`.js` under `scripts/` (85 files) and runs **first** in the gate list — cheapest check, most expensive failure to find in production. Executes nothing: no network, no writes, no secrets. Verified it flags the exact file and line that shipped |
| `ab0109b` | **Toyota MSRP now comes from the published national table — and my own previous entry had the diagnosis half wrong.** `2d43c04` correctly stopped storing `vehicleStartPrice` (province-calculated, 12 values across 13 provinces) but concluded no alternative existed. It did: `/bin/api/price_calculation/from_prices.<BRAND>.<PROVINCE>.json` returns an `MSRP`/`PDSF` line identical in every province, with a fee stack that reconciles to the printed subtotal — which is what makes the basis knowable as ex-freight. I missed it because I searched the served HTML, and the client builds that URL at runtime | green signal, no check | base trims read the published line; other trims come from a difference that is published **only if ON, AB and QC all yield the identical whole-dollar figure** — disagreement means a province-specific amount survived, so the row is refused (Sequoia and GR Supra are refused live). Reproduces all four hand-seeded RAV4 PHEV values exactly. Internal grade codes (`BX`, `WX`, `HI`) refused as trim names since they match no listing. 89 rows / 24 models, vs 7 wrong ones before. `test:catalog-writes` pins the provenance; 6 assertions fail against pre-fix main |
| `2d43c04` | **Toyota's "MSRP" was a province-calculated price — and the quality gate ADMITTED some of it.** `vehicleStartPrice` returns 12 distinct values across 13 provinces for one Land Cruiser, so it is definitionally not a national MSRP; Lexus is identical. The whole-dollar gate was not merely too strict, it was the wrong shape: AB/BC/NL/YT/NT all return whole dollars that disagree, so at `province=ON` it passed 7 of 76 rows into `msrp_catalog` as manufacturer prices. The prior Open note said "the gate is correct; the field is wrong" — the rejection was doing the visible work while the acceptance did the damage | green signal, no check | **superseded by `ab0109b` — the "no re-sourcing exists" conclusion in this row was WRONG.** I grepped the served HTML for `/bin/api/` paths and found nothing; the Build & Price client assembles `from_prices.<BRAND>.<PROVINCE>.json` at runtime, so it never appears in the markup. The stopgap (rates-only) was safe but left a real published source unused. Hand-seeded Build & Price rows keep their known basis; rates keep refreshing. `test:catalog-writes` pins it, including that the province evidence stays in the file |
| `2d43c04` | **`5f4259d` converted twenty scrapers to `writeCatalogs()` and missed two.** `tci-stack` (Toyota, Lexus) and `fca-stack` (Jeep, Ram, Dodge, Chrysler) kept hand-rolled three-table writes, so an MSRP throw still discarded the finance and lease rows already in hand — which is why *the very Toyota run that motivated 5f4259d* was still losing its rates. `fca-stack` carried a second copy inside a per-make loop, so a collapse on the first make silently cost every make after it, MSRP and rates alike | one-surface fix | both write through `writeCatalogs()`; fca wraps each make so one failure cannot end the loop, collecting failures and rethrowing together. `test:catalog-writes` forbids any `replaceRows("msrp_catalog", …)` outside the helper — all 7 assertions fail against the pre-fix files |
| `d311079` | **95 emoji were doing icons' jobs across `src/App.jsx` and five static pages.** An emoji renders as whatever glyph the device ships, so the same signed report rendered differently on Android than on macOS — on the one artifact whose value depends on the buyer and the dealer looking at the same thing. Three of the 95 could not take an element at all: two report titles and the AMVIC label are **also** `explainFor` object keys and HTML `title` attributes, so JSX there would have broken a lookup and printed `[object Object]` in a tooltip — those lost the glyph instead, which `tone:"flag"` was already saying in red. `seal.gl` was deleted outright: nothing ever read it and its only content was an emoji | — | `src/icons3d.jsx` — 34 icons, each lit from one source with a turned face and a contact shadow, each animating in a way that describes its subject, all stopping under `prefers-reduced-motion`. The stylesheet **injects itself on import** rather than being mounted per surface, because icons render on five roots and a surface that forgot to mount it would render every icon frozen — invisible in review. `check:no-emoji` blocks pictographs in shipped UI and names the allowed typographic marks in its failure. `test:icons3d` catches the silent one: **a gradient id that does not resolve paints NOTHING**, so a broken warning icon reads as "no warning here" — every `url(#…)` must resolve inside its own icon, every animation must be declared and reduced-motion safe, every icon must be lit, move and be used, and `public/icons3d.css` must not have drifted from the module. Mutation-checked |
| `69d77b4` | **The live-dot guard checked one call site, not every one — and I shipped it that way.** PR #220 conflicted with `383532a` on the exact line destructuring `useApiUsage`, and GitHub's "Accept current change" resolves it to the pre-change version: `lastReadAt` dropped in AdminPanel, kept in FoundersPanel, so that panel's badge can never light. **Every existing contract stayed green against that resolution** — the hook still exported it, the badge still consumed it, nothing read `!apiUsageLoading`. A value threaded correctly through one consumer and dropped in another is invisible to a check that only asks whether the wiring exists *somewhere*. Resolved the conflict as a union in `c62b1ae`; neither button was correct, the same trap as #161 | green signal, no check | `test:live-dot` now **counts** the wiring: every `useApiUsage()` call site must destructure `lastReadAt` and every `<VerificationTab>` render must be passed `apiUsageReadAt`, with the offending lines printed. Both checks fail loudly if their pattern matches nothing, so the gate cannot go blind by code moving. Verified against a reconstruction of the bad resolution: 25 passed, 1 failed |
| `dcee2af` | **`canonicalReport()` had already drifted, and only a code comment was guarding it.** `4e3a733` ("Flag when the advertised price depends on financing with the dealer") added `fcx` and `source` to the CLIENT copy and never touched `report-sign.ts`. The server **computes** `financeContingent` on both analyze paths and simply never projected it, so the finance-contingent flag — a dealer tactic the product exists to surface — **could not appear on /verify for any signed report**. Same defect the v2 bump was fixing, two fields further down. Not a signature break: the server signs its own copy and `finalizeReport()` returns it verbatim, so the client copy only serves the explicitly-unsigned fallback | one-surface fix | `fcx` + `source` projected server-side verbatim from the client, `v2`→`v3` on both. `check:canonical` parses the returned object literal on each side (brace-depth aware, skipping strings and comments) and compares top-level keys **in order**, since `JSON.stringify` emits insertion order. Verified against both failure modes — a missing field and same-keys-wrong-order — and it fails loudly if it parses zero keys, so it cannot go blind |
| `107250f` | `/verify` showed a bare leverage score and flagged fees with no basis, though the backend already computed `leverageScore.note` and `addOn.reason`. The signed canonical record simply never carried them | absence read as knowledge | `v1`→`v2` adds `lvn` + per-add-on `reason` on **both** copies. Old links unaffected: `runVerify` re-hashes the bytes embedded in the link and never rebuilds `canonicalReport()`, so a v1 link verifies exactly as issued and renders with `o.lvn &&` short-circuiting |
| `383532a` | **The verification ledger showed "● Live" over a read that failed.** It lit on `loaded = !apiUsageLoading`, and a FAILED read clears that flag too — `useApiUsage` catches, logs a warning, and clears loading in `finally` either way. So the badge claimed live over data it never received | green signal, no check | the hook reports `lastReadAt`, stamped only beside `setUsage` inside the `try`; a timestamp cannot be produced by a failure. `test:live-dot` asserts no badge is gated on `!apiUsageLoading` |
| `383532a` | **LiveTicker blinked a green dot over `DEMO_LISTINGS`** — fourteen invented cars at invented prices, on the public landing page. Its guard was `listings.length > 0`, which **can never be false**: `useListings` *seeds* its state with `DEMO_LISTINGS`, so the demo array arrives through that same prop and the local fallback was dead code. The real answer already existed one hook away as `isLive`, set only inside `if(data && data.length > 0)` — it was simply never passed down | green signal, no check | gates on `isLive`; demo rows lose the dot and carry a "Sample" chip (not green, not animated — it is the opposite claim). `test:live-dot` asserts the gate AND that `isLive` is actually passed, since a prop nothing supplies leaves the gate permanently false |
| `383532a` | `VerifLiveDot` **could only render lit.** It took a colour and nothing else, so every caller had to remember to gate it — and the one caller gated it wrong. The component that cannot express "not live" is the defect, not the call site | one-surface fix | deleted, not fixed. Replaced by `src/lib/live-state.js` — one decision function whose only input is `readAt`, with no argument that forces a lit result, and three states so `aged` is neither hidden nor called live. 14 unit tests incl. a FUTURE timestamp reading `unavailable`, so a skewed clock cannot pin a badge lit |
| `7979db0` | **The Alberta map invented dealer counts and called them live.** `alberta.html` held a frozen per-city snapshot so a failed fetch could "fall back silently … so the map always renders". It summed to **527 against a real 405**, disagreed with OSM in **30 of 33 cities**, and showed Grande Prairie **24** where OSM finds **2** — under a hardcoded `Live dealers` badge, reached through three silent exits. One dealer screenshot of that discredits the product | absence read as knowledge | counts deleted from markup; `dealers` is null until a real read and every path renders "—"; badge ships neutral and JS lights it only after a successful read (live / aged / unavailable); `test:alberta-dealers` — all 5 markup assertions fail against the pre-fix file |
| `7979db0` | Overpass answered **HTTP 406 to every mirror** because Node's fetch sends no descriptive User-Agent, which the API's policy requires. Four mirrors × four retry rounds could never have helped — the request shape was the bug, and Overpass was healthy throughout. 4 of the last 5 weekly runs lost to it | green signal, no check | descriptive `User-Agent` with a contact URL; `check:jobs` requires one on every third-party fetch in `scripts/` — it caught two more silent cases (StatCan, NRCan), both fixed in the same commit |
| `7979db0` | The failure **always blamed the wrong mirror**. Only `lastErr` survived, so the error described whichever ran last — `overpass.osm.ch`, which fails *differently* (200 + empty body). Four runs printed "only 0 elements" and the 406 never appeared once. A report naming one of four failures is worse than none: it looks like a diagnosis | absence read as knowledge | every mirror's outcome collected and printed, de-duplicated, with an explicit "406/429 across the board means REFUSED, not down" note in the thrown error |
| `7979db0` | **A daily job that had never run.** `update-statcan-zev.yml` sat in a **dotless** `github/workflows/`. GitHub only reads `.github/workflows/`, so it never appeared in `gh workflow list`, never ran, and **never failed** — 46 days of a daily schedule, with `CLAUDE.md` documenting it as active. The script was fine; it passed first try once moved | green signal, no check | `check:jobs` fails on any workflow outside `.github/workflows/`; verified by re-creating the dotless directory and watching it catch |
| `7979db0` | The refresh **floored two numbers and wrote a third**. `els` and `dealers` were checked; `assigned` — the count actually written — was not. 700 valid dealers all outside `MAX_KM` pass both and commit `"totalDealers": 0` at exit 0. The absolute floor of 50 is ~12% of a normal run, so 405 → 51 also commits green | green signal, no check | `assertOutputSane()` in `scripts/lib/dealer-output-guard.mjs` — absolute floor **plus** a >50% collapse refusal measured against what is on disk, reusing the catalog-refresh rule rather than reinventing it |
| `f2a5b96` | In the emailed PDF a muted row rendered faded title + faint value + soft body, so the whole row receded — worst on "Financing math · $2,075/MO REF", the most actionable number on the page and the hardest line to read on it. The on-screen views colour only the DOT by tone and leave titles alone, so the PDF was the odd surface out | one-surface fix | the audit LABEL always renders in `INK`; only the VALUE carries tone |
| `d48aaca` | RAV4 Woodland reported `$47,000 · starting at` when the catalog holds its exact price. **Not a matching bug** — the null drivetrain was deliberate (no Build & Price summary states AWD/FWD, so the seed refused to guess) and `rowConfirmsConfig` was right to refuse `exact`. The real cause: NRCan pinned `rav4 hybrid → AWD` but not plain `rav4`, and the report resolved to the un-pinned key. Second cost of the same alias pair, after the $6,299 floor | one-surface fix | enrichment propagates symmetrically across the alias pair — NULLs only, idempotent, in whichever direction the knowledge arrived |
| `3fec6a1` | Changed `analyze-quote` (analysis output) without bumping `CACHE_VER` — every cached report would replay old figures while the report id stayed the same, reading exactly like a failed deploy | green signal, no check | `check:cache-ver` caught it **on CI**, not locally; the gate diffs against the merge base |

## 2026-08-16

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `3596e88` | **All 13 verification checkpoints read "read failed" on every rolling window — and nothing above them was wrong.** `buildVerifIntervals` gave the in-progress bucket JS's maximum Date as an open-ended sentinel. That value becomes `p_until`, and the maximum Date serialises to `+275760-09-13T00:00:00.000Z` — an extended year Postgres will not parse, so it rejected the call outright. The grants, the migration, the admin identity and the function signature were all correct and **each looked equally guilty, because a failed read says nothing about which layer failed**. I shipped one fix on an unverified diagnosis before this (#204) and reported it resolved when it was not. What finally located it was the split: the calendar view worked, and its builder always set a real end | one-surface fix | `pgTimestamp` clamps at the boundary. A read window is the one place clamping is the right call — too wide a window still returns exactly the right rows — so a bad Date now degrades to **correct** instead of killing the read, whatever route it arrives by. The maximum-Date sentinel is banned from `src/` outright so the habit cannot return through another builder. `test:pg-timestamp` runs against the **real function extracted from `App.jsx`**, since a test that restates the code proves only that the copy agrees with itself; mutation-checked — restoring the sentinel fails two independent rules |
| `1861202` | `analyze-quote` dropped `term_months` from the manufacturer rate and never computed reference financing — the identical line already fixed in `analyze-listing-url` | one-surface fix | — *(see Open, below)* |
| `5f4259d` | A refused MSRP write threw and skipped the finance + lease writes. 123 finance and 120 lease rows were ready and discarded because an unrelated table failed its guard | optional step, fatal failure | each table written independently; failures collect and rethrow together |
| `d638e91` | `ERR::ASP::SHIELD_PROTECTION_FAILED` treated as terminal. A blocked attempt costs **0 credits** and the exit geography varies per attempt, so giving up after one was free to avoid | green signal, no check | shield failures re-roll up to 3× within budget; 422 deliberately excluded |
| `2dc0c1f` | An optional manufacturer-MSRP lookup threw an `AbortError` and killed a scan that already had price, VIN, trim and recalls. Separately, a 422 on the fullpage screenshot returned null and never tried the viewport | optional step, fatal failure | `enrichAnalysis` guarded at the boundary (covers all six call sites and any future enricher); screenshot degrades on *any* failure |
| `9b9ba75` | `RAV4` (7 rows, floor $41,361) and `RAV4 Hybrid` (4 rows, floor $47,660) — same car, two keys, a point-in-time `INSERT…SELECT` that nothing re-ran. The report's floor depended on which name the lookup hit | green signal, no check | resync is total and idempotent: adds missing **and** deletes orphans |
| `c0eaeae` | Days-on-lot vanished from Sidebar, Scroll and Heatmap when the platform published no date. The **email had already been fixed** for exactly this | one-surface fix | always renders "Not published — ask the dealer"; heatmap no longer labels it "0 days" |
| `08ec24c` | One panel told three stories: "from $47,660", "$52,000 when new", "STARTS at $52,000 for the base version" — the last false twice over | absence read as knowledge | explanation defers to `qualifyMsrpClaim().refusal`, the same source the email already used; range scoped to trims we actually hold |
| `9255f62` | The counter-script told an Alberta buyer to cite "the FTC CARS Rule in the US" | — | `check:copy` extended to server-side copy; new rule blocks foreign-regulator citations |
| `a790eb4` | Dealer reputation said "NOT CHECKED" for a dealer with 3,369 Google reviews — the lookup was a browser-side progressive enhancement the report raced | absence read as knowledge | resolved server-side before any surface is built; city recovered from the hostname |
| `0f95d97` | Financing math printed "NO TERMS QUOTED" while both halves sat in our own tables. `resolveFinanceRates` selected a row carrying `term_months` and returned it without | green signal, no check | term carried; `computeReferenceFinancing` amortizes both prices at the same published rate |
| `d7b475b` | The Scrapfly render asked for `render_js` + `auto_scroll` + a **fullpage** screenshot in one call. A timeout lost the HTML — the part that actually rescues a page | optional step, fatal failure | viewport shot (bounded by construction) then an HTML-only retry |
| `d6610d6` | Vision rescue posted whatever screenshot the render produced and took an HTTP 400. A 17,729px capture is past the API ceiling *by construction* | green signal, no check | `visionImageVerdict()` decides before the request; the 400 body is logged |
| `ba36a38` | Two migrations inserted into tables created by later-sorting files — the history could not rebuild from empty | green signal, no check | renamed so filename order matches dependency order; allowlist deleted rather than grown |

## 2026-08-15

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `71848f3` | Three false claims in one shipped report: "no public reviews" about a dealer with 5,930; a rebate contradiction the prompt itself caused; "$11,173 over MSRP" when the true gap was $8,095 | absence read as knowledge | `point-state.ts` three states; `settled-claims.ts` strips post-generation; jurisdiction resolved from every signal, refusing when unknown |
| `aa6183a` | Five seeded models were unreachable by the lookup, and **"Crown Signia Limited" resolved to "Crown"** — a different car that also has a "Limited" | green signal, no check | `test:reachable` — every seeded model must resolve to itself or to null |
| `d3dcc43` | Two rate seeds had been failing with 42703 since the morning; the APR half of the product had no data behind it | green signal, no check | `check:migrations` replays schema in filename order, catching 42703 **and** 42P01 |
| `2b51a2d` | Superseding a hand-verified row blanked the enrichment it carried, and the row sat outside the restore set — so a failed insert reported "restored all N" while it was gone | green signal, no check | `test:supersede` runs `replaceRows` end-to-end; its stub honours the query filter |
| `d05ca03` | A premium colour hid inside the MSRP line with no package line to flag it — the third nameplate in a row | one-surface fix | `bp-summary.mjs` refuses on **both** routes (package suffix *and* exterior) plus reconciliation |
| `a959d24` | #161 had sat conflicting for three days; "Accept current change" would have reverted the collapse guard | — | union merge: both fresh-write detectors, dedup moved ahead of the collapse check |

---

## 2026-08-12

| fix | what broke | class | guard now in place |
|---|---|---|---|
| `f377fc5` | **A genuine seal could be transplanted onto forged report content.** `verifySealedShot` proved the image was sealed in SOME validly-signed canonical but never bound that canonical to THIS analysis — the printed report id, vehicle and prices all came unverified from the client, so keeping a real (verifyPayload, sig, keyId, listingShot) quad and rewriting every other field printed "SEALED LISTING CAPTURE … captured for report <fake>" onto fabricated content. Same commit closed three siblings: the email's main CTA href was the raw client `reportUrl` (a phishing link in a DKIM-signed LotCheck email — `verifyUrl` had the lotcheck.ca allowlist, `reportUrl` did not), gzip decompression ran unbounded BEFORE the signature check on an unauthenticated endpoint (OOM lever), and `pngPixelCount` read "dimensions" from whatever chunk came first, so a small decoy chunk bypassed the pixel-bomb budget | one-surface fix | the canonical's own fingerprint must equal `analysis.reportId` or the capture drops; caption facts (report id, listing URL) print from the VERIFIED canonical only; `reportUrl` gets `verifyUrl`'s exact allowlist; gunzip reads under a 2 MB budget with writer rejections caught; the first PNG chunk must BE IHDR — pinned in `capture.test.ts`. Found by the ship review army + red team (43 findings, 8 critical; 13 fixed same day), the rest tracked in TODOS.md |
| `9ce72d8` | **A failed market read rendered as "Awaiting Alberta listings — 0 of 25 needed" — a fabricated market count** — and the catalog read had a built-in outage date: `limit=5000` under Supabase's silent `db-max-rows` cap plus a hard throw meant the page would brick itself the day the catalog crossed 1,000 rows, while an unreadable Content-Range header made the partial-read guard silently skip in exactly the configuration it was built to catch | absence read as knowledge | three distinct non-data states (loading / read-failed / below-the-gate) so an error can never render as a listing count; the catalog read walks offset pages until an EMPTY page structurally proves exhaustion (immune to any server cap), with Content-Range demoted to a cross-check; RPC responses are shape-validated so schema drift lands in the error state instead of NaN-cascading into empty charts. Structural in `public/live-price-index.html`; the extract-to-module node gate for the band math is an open TODOS.md item |

## Open

### Found 2026-08-27, not yet fixed

A 49-agent root-cause pass over the Lexus NX report confirmed 30 defects (44
candidates, 14 refuted by adversarial re-verification). The five above are
fixed. These are the rest, recorded so they are not rediscovered a third time.

- ~~A platform's addon caption must never classify money.~~ **Closed by
  `38274c2`** — and it was not a latent risk, it was live: the counter-script
  told the buyer to demand the dealer remove $3,330 that is 70% freight and
  government levies. ~~Blocked on real freight per model.~~ **Unblocked by
  `32903cd`** — the stack is now captured from the manufacturer, reconciled to
  the cent, and refused when it cannot be proven.
  **STILL OPEN from this item, and it is the useful half:**
  - **No surface RENDERS the decomposition yet.** The data exists for
    Toyota/Lexus (`all_in_price` + `attrs.all_in_breakdown`) and nothing reads
    it. The buyer is told to ask the dealer to itemise the bundle; we can now
    often tell them the answer instead. That card is the next build, and it must
    land on every surface at once.
  - **`explainAllIn()` still has ZERO call sites.** The capture supersedes what
    it was for on Toyota/Lexus; decide whether to point it at the captured rows
    (with the hardcoded catalog as fallback) or delete it. Leaving a second,
    unverifiable fee catalog beside a captured one is how the tire levy went
    stale in the first place.
  - **`fee-schedule.ts`'s hardcoded Alberta tire levy is $25; the manufacturer
    publishes $20 for 7 of 18 Lexus series**, including the NX in the customer's
    report. `bp-summary.mjs:52` hardcodes it too. Both are now falsifiable
    against captured data.
  - **`all_in_price` is not in CARRY_FORWARD**, so a make whose scraper does not
    supply it still has hand-seeded values blanked on refresh. One line — but it
    breaks `test-carry-forward.mjs`, which uses `all_in_price` as its example of
    an UNCOVERED field. Re-point that test at a synthetic column name first, or
    layer-2 coverage is silently lost.
  - **Only Toyota and Lexus are captured.** Stellantis (`fca-stack.mjs`),
    Volkswagen (`scrape-vw.mjs`, which already reads `freight_pdi` and discards a
    10-province `taxes_and_fees`) and Mazda (`scrape-mazda.mjs`) each publish the
    same two numbers in a request already being made — ten makes, four files,
    zero new HTTP.
- ~~The report gives four different answers to "how many trims does the
  manufacturer publish".~~ **Closed by `501a9cd` + `4c5f39d`** — one shared cap,
  every surface printing "Showing 12 of 17", and the count computed over the
  full ladder rather than the truncated view.
- **The MSRP-per-trim card travels OUTSIDE the report signature** and cannot
  appear on `/verify` at all, so the one card a dealer would most want to
  dispute is the one we cannot prove.
- ~~`encodeReport` drops 8 canonical fields.~~ **Closed by `b26a535`** — every
  field carried, an unconfirmed recall can no longer forward as confirmed, and
  two gates now police the share link (`check:canonical` for top-level drops,
  `test:share-round-trip` for the nested ones it structurally cannot see).
- **The Book (flipbook) carries no report ID, no verify link, no seal and no
  capture** — the only "printed" surface with nothing to verify against.
- ~~Scroll never renders the sealed listing capture, and the parity gate has no
  entry for the Evidence panel.~~ **Closed by `8401c99`** — the gate is
  range-aware (it certified Scroll off ReportViews' text for weeks), Evidence is
  one shared component every surface mounts, and the heatmap/sidebar pools are
  one pool. **STILL OPEN from this item:** the financing payment matrix
  (`FinancingBreakdown`) still has exactly ONE call site in the whole app — it
  renders on Scroll and nowhere else.
- ~~The heatmap heading, the "point 12 / 14" numbering, and the advertised ten
  not matching the delivered ten.~~ **Closed by `01a9d02`** — one canonical list
  in `_shared/report-points.js`, two labelled bands on every surface, ordinals
  read off each item's own `point` flag, and `check:points` reads the arrays
  instead of counting the phrase.
- **A partial golden-set rebuild overwrites the baseline.** The killed benchmark
  left `answer-keys.json` holding 105 of 120 keys with 15 fetch failures
  recorded in its own `meta`, sitting where the 120-key baseline belongs. Same
  class as [[catalog-refresh-can-empty-catalog]]: a run that could not do the
  whole job must not overwrite the thing it half-rebuilt.


- **`msrp_catalog` likely holds true duplicate rows for the same trim, not
  just display-layer clutter.** `0c8342f` (same day) fixed the SYMPTOM —
  the "MSRP per trim" panel rendering every row with no dedup — but the
  underlying catalog data itself wasn't touched, and the same panel still
  shows genuinely conflicting entries for a similar trim name at DIFFERENT
  prices (2026 RAV4: "LIMITED" $52,000 vs "Limited" $52,350; "XSE" at both
  $50,900 and $56,400) that the display fix correctly declined to silently
  resolve. Worth a real data-hygiene pass on the catalog refresh/upsert
  path — what constraint (if any) prevents a re-run from inserting a second
  row for the same make/model/year/trim, and are the casing-variant rows
  ("LIMITED" vs "Limited") actually the same trim from two source runs that
  never normalized to one key.
- ~~A report can claim its own extraction failed entirely while its OWN
  sealed screenshot proves the data was there and readable.~~ **Closed by
  `dfa00f7`, same day.** `LC-B4CF-D00` (legacyautogroup.ca, 2026 Ford
  Explorer, 2026-08-20): the report's "Bottom Line" stated the captured page
  content was only nav/footer/CARFAX boilerplate, "Price vs MSRP" fell back
  to a catalog "starting at $54,895" when the dealer's own page states MSRP
  $75,015, "Financing APR" fell back to a 4.99% manufacturer reference when
  the page states 5.49%, and the odometer read 0 km against a real 508 —
  yet the report's own sealed screenshot, attached to the same signed
  report, clearly shows all of it. The page loads via a JS-heavy SPA that
  returns near-empty boilerplate to a plain fetch (confirmed directly: 111KB
  of real content stripped from a 1.04MB raw response) — exactly the case
  the vision rescue exists for. First landed as an Open item with only
  diagnostic logging (`a77052f`) because the exact mechanism wasn't
  confirmed from real logs; pushed to an actual fix the same day. Root
  cause: `enrichAnalysis()` was always handed the FULL remaining
  `REQUEST_DEADLINE`, with nothing reserved for the vision-rescue step that
  runs after it — on a slow listing, enrichment (recalls, catalog MSRP,
  dealer reputation, financing lookups) could spend the entire budget,
  leaving rescue's own calc clamped to its 1-second floor by the time its
  turn came. Fixed by reserving rescue a 40s minimum BEFORE calling
  enrichAnalysis, using enrichAnalysis's own existing deadline-awareness (it
  already self-skips expensive sub-steps near its deadline) rather than
  adding new skip logic — total budget unchanged, just no longer let one
  step starve the other with no coordination between them. Not
  independently verified against a live Scrapfly-enabled run; the diagnostic
  log from `a77052f` stays in place so the next occurrence (if any) confirms
  or corrects the theory from real timing data.
- ~~Toyota scraper reads a calculated price.~~ **Closed** — but the note above
  was wrong in a way worth keeping. It said *"the gate is correct; the field is
  wrong."* The field was wrong; **the gate was wrong too.** A whole-dollar value
  is not evidence of a published MSRP: the same Land Cruiser returns twelve
  distinct values across thirteen provinces, and AB/BC/NL/YT/NT are all whole
  dollars that disagree with each other. At `province=ON` the gate therefore
  ADMITTED 7 of 76 rows — every one an Ontario-calculated figure headed for
  `msrp_catalog` as a manufacturer price. The rejection was doing the visible
  work while the acceptance did the damage.
  It also could not be fixed by re-sourcing: Toyota and Lexus publish no
  machine-readable national MSRP anywhere reachable (prices.json, the
  BnP-get-models fragment, the series list, calculator.json, the B&P page and
  the vehicle overview page were all checked). So the platform now writes none.
- **`country=ca` is not taking effect at Scrapfly.** Re-rolling makes it
  survivable, not deterministic.
- **`check:parity` checks report *surfaces*, not shared *helpers*.** It did not
  catch the dropped-term bug living in two functions. Extending it would close
  the one-surface class properly.
- ~~Lit "Live" dots outside the Alberta map.~~ **Closed by `383532a`** — the
  shared primitive named here now exists (`src/lib/live-state.js`) and both
  instances route through it. Kept as a note because the *shape* is worth
  remembering: all three surfaces failed the same way, by substituting something
  merely **correlated** with a successful read for the read itself — static
  markup, `!apiUsageLoading`, and `listings.length > 0`. When a live badge is
  wrong, look first for the cheap proxy standing in for the real signal, and
  check whether the honest answer already exists nearby. It did in both cases:
  `useApiUsage` could have reported when a read returned, and `useListings`
  already computed `isLive` and simply never passed it down.
- **RAV4 and RAV4 Hybrid are still two keys for one car.** The resync converges
  their rows and `20260817` keeps their enrichment in step, but neither removes
  the duplication. The pair has now cost two distinct defects — a $6,299 wrong
  floor and an unearned `starting_at`. The permanent fix is to resolve both
  names to ONE row set in the matcher; it interacts with the powertrain guard,
  so it is a design call, not a patch.

---

## How to add an entry

Only add a fix once its guard exists. A row here without something in the
`guard` column is a note, not a fix — see the memory `fix-means-structural-fix`.

**Use the real commit hash, never a placeholder.** A row that says `PENDING`
looks complete in a scan and traces to nothing, which is the same failure this
whole file is about: a green signal with no check behind it. Write the row
*after* you commit, or come back and fill it.
